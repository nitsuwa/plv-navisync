# PLV NaviSync — Developer 2 Progress

This is the live checklist for Workstream B — Map Authoring and Navigation. The version on `main` is official. Check a package in the same Pull Request only after its implementation and required tests are complete; it becomes `DONE` when that Pull Request is merged.

Status values: `READY`, `ACTIVE`, `BLOCKED`, `FOR REVIEW`, `DONE`.

## Package checklist

- [x] **B1 — Map Builder baseline audit and regression protection**
  - Status: `DONE` — merged through Pull Request #5 (`test/map-builder-baseline` → `main`, merge commit `b19d974`).
  - Branch: `test/map-builder-baseline`
  - Depends on: None
  - Test result: 171 passed (17 files) — `pnpm test`; build passed — see `DEVELOPER_2_B1_VERIFICATION.md`
  - Pull Request: #5 (merged)
- [x] **B2 — Outdoor campus canvas and object authoring**
  - Status: `DONE` — merged through Pull Request #6 (`feature/outdoor-campus-editor` -> `main`, merge commit `0685632`).
  - Branch: `feature/outdoor-campus-editor` (branch in use; roadmap lists `feature/outdoor-campus-authoring`)
  - Depends on: B1 (merged)
  - Scope delivered: Phase 1 multi-object group movement · Phase 2 layer ordering · Phase 3 decorative asset property controls · Phase 4 decorative asset visual polish
  - Test result: 38 files / 329 tests passed — `pnpm test` (B2 added 9 test files / 82 tests: 54 pure + 28 integration, plus this pass's create-campus regression suite); `pnpm build` passed (pre-existing >500 kB chunk warning only); `git diff --check` clean
  - Pull Request: #6 (merged)
  - Create Campus manual retest: passed before B3 proceeded.
- [x] **B3 — Building configuration and entrances**
  - Status: `DONE` — merged through Pull Request #7 (`feature/building-entrances` -> `main`, merge commit `7b4ba74`).
  - Branch: `feature/building-entrances` (roadmap lists `feature/building-entrance-authoring`)
  - Depends on: B2; A5 final normalized entrance integration remains deferred because B3 intentionally avoided a new DB migration.
  - Scope delivered: building-attached entrance model using edge + normalized offset geometry; rotation/resize-safe attachment; entrance add/select/edit/delete/reposition in the editor and PropertiesPanel; General Entrance / Service Entrance / Emergency Exit purpose semantics; one Primary General entrance per building; Accessible entrance property; primary deletion auto-promotes another General entrance when available; validation distinguishes no entrance versus no primary entrance; duplicate-building entrance cloning with remapped parent IDs; locked/hidden behavior; undo/redo/history support for entrance actions; shared snapshot/adapter compatibility; campus-card building count fixed on initial load; campus-card lightweight visual preview fixed on initial load; Buildings/Floors/Rooms/Markers metric tooltips.
  - Test result: 42 test files / 388 tests passed — `pnpm test`; `pnpm build` PASSED with only the existing non-blocking >500 kB `AdminMapBuilderPage` Vite chunk warning; `git diff --check` PASSED.
  - Manual verification: PASSED, including final campus-card preview check.
  - Persistence note: entrances currently persist through the editor state, building metadata in the current structure save/load path, sanitized/shared published snapshots, campus cloning, and public adapter compatibility. B3 does **not** add a dedicated entrance table or new DB migration; final normalized DB-backed entrance integration remains gated/deferred to the later A5/B6 persistence contract.
  - Pull Request: #7 (merged)
- [ ] **B4 — Floor-plan authoring**
  - Status: `COMPLETE` — B4 Floor Editor Overhaul implementation AND the final manual QA gate have PASSED (August 10, 2026); the B4 checkpoint commit `feat: complete floor editor overhaul` was created on `feature/floor-editor-overhaul` (merge to main / push still require an explicit owner decision). Phase 1 foundation, Phase 1.5 floor editor UX/interaction parity, Phase 1.6 floor editor UX completion/outdoor parity polish, Phase 1.7 floor editor final UX polish, Phase 1.8 interaction refinement, Phase 1.9 visual/interaction polish, Phase 2.0 transform / multi-selection / property UX completion, Phase 2.1 floor object layering / visibility / lock + room / wall / circulation polish, Phase 2.2 room/wall attachment + floor-boundary visual fixes + wall authoring UX, Phase 2.3 robust room/wall attachment + wall-attached doors/windows + floor/canvas resize semantics, Phase 2.4 door/window usability + shortcuts + room label readability, Phase 2.5 manual QA corrections, Phase 2.6 door/window visual correction + selection boundary fix, Phase 2.7 opening clipping + perimeter interaction + deselect fix, Phase 2.8 opening corner safety + orientation/resize + perimeter editing + property UI polish, Phase 2.9 door width consistency + door types + perimeter entrance validation, Phase 3.0 managed perimeter UUID persistence + Double Door visual correction, B4 Phase 3.1 Double Door visual correction, floor view/default cleanup, B4 text annotation UX + Floor Settings modal fixes, B4 text/modal manual-QA fixes, the B4 live text editing + floor management workflow, the B4 floor management UX consistency pass, the B4 final validation / floor-editor dirty-state integration pass, and the B4 final correction — wall creation structural snapping are all delivered; the combined manual final-gate retest has passed and B4 is COMPLETE.
  - Branch: `feature/floor-editor-overhaul` (roadmap lists `feature/floor-plan-authoring`)
  - Depends on: B3 (merged)
  - Scope delivered so far: Phase 0 audit/planning; Phase 1 floor data normalization, canonical floor defaults, editable floor canvas dimensions, legacy/incomplete floor loading safety, safe floor switching with unsaved-change guard, save/discard/cancel switching behavior, dirty-state reset on successful save, failed-save preservation of dirty edits, transient selection/drag/drawing cleanup on floor switch, history reseed on floor switch, and current structure serialization/hydration compatibility for normalized floor collections; Phase 1.5 visible Select/Pan controls, explicit More tools menu, Ctrl+wheel zoom, Fit Floor control, marquee selection, multi-object group drag, room resize boundary clamps, object placement/movement clamps, canvas-size panel with shrink validation, floor-specific shortcuts modal, and lightweight furniture shape rendering without visible canvas text labels; Phase 1.6 canonical `0,0,width,height` floor surface, world-space empty state, decorative compass removal, docked/auto-opening properties inspector, visual furniture palette previews, bounded furniture resize handles, architectural wall rendering with boundary/endpoint snapping, floor-specific right-click duplicate/delete/properties menus, compact Issues popover, and hidden Measure tool until a real measuring workflow exists; Phase 1.7 expanded multi-selection across rooms/furniture/stairs/ramps/elevators/labels/doors/windows/walls with marquee and modifier-click selection (floor paths intentionally excluded), group drag that keeps relative layout inside floor bounds, group delete/duplicate as single history actions with new IDs and rigid offsets, group-aware right-click menus, wall connection snapping with priority endpoint → segment → floor boundary and a subtle transient snap indicator that clears on completion/tool switch/floor switch, curated campus-relevant furniture library with a shared `FloorFurnitureSymbol` renderer for palette previews and placed objects, realistic neutral furniture colors, per-type resize ceilings (min 8, chair 32, sofa 86/44, desk 74/48, etc.) with safe legacy fallback, Floor Save control with real loading state/duplicate-submission lock/dirty-clear-on-success/dirty-preserve-on-failure, gated Publish control that delegates only through the existing campus publish flow (no independent floor publication state), Canvas Settings draft Width/Height editing with Save Changes/Cancel and shrink blocking, and wall/canvas boundary consistency preserved across all object types.
  - Test result: Phase 1 through 3.1 plus floor view/default cleanup, text annotation UX/Floor Settings modal, live text editing + floor management workflow, floor management UX consistency, final dirty-state integration, and wall creation structural snapping verification has passed so far. Latest full suite passed with 53 files / 599 tests in `pnpm test`; latest `pnpm build` passed with the existing non-blocking >500 kB `AdminMapBuilderPage` Vite chunk warning; latest `git diff --check` clean.
  - Deferred: floor background upload/storage/calibration, connector linking, stairs/elevator transition routing, B5 navigation/pathfinding, outdoor editor changes, full wall topology, and database migrations.
  - Pull Request: Pending
- [ ] **B5 — Navigation graph authoring**
  - Status: `ACTIVE` — Phase 1 (Navigation Graph Foundation + Basic Outdoor Node/Edge Authoring), Phase 1.5 (Navigation Authoring UX / Tool Architecture Correction), Phase 1.6 (Navigation Foundation UX Finalization), Phase 1.7 (Outdoor Navigation Manual-QA Corrections), Phase 1.8 (Entrance-Linked Navigation Final Corrections), Phase 1.9 (Entrance Navigation Visual Cleanup), Phase 2 (Indoor Navigation Authoring Foundation), Phase 2.1 (Floor Editor Navigation UX / Shortcuts / Tooling Correction), Phase 2.2 (Indoor Navigation UX Simplification + Interaction Fixes), Phase 2.3 (Indoor Navigation Multi-Select + Placement Rules + Circulation Visual Polish), Phase 2.4 (Indoor Navigation Tool Simplification + Route Anchor + Circulation Visual Corrections), Phase 2.5 (Segmented Paths + Design Nav Overlay + Ramp Visual), Phase 2.6 (Connect Feedback + Path Bend UX + Overlay Semantics + Ramp Polish), and Phase 2.7 (Orthogonal Connector UX + Connect Hover Fix + Centered Linked Anchors) are implemented on `feature/navigation-graph-authoring` after the audit pass; B5 remains incomplete until later phases (cross-floor stairs/elevator links, entrance indoor↔outdoor links, route testing, student routing, accessible route computation, SOS pathfinding) are delivered.
  - Branch: `feature/navigation-graph-authoring`
  - Depends on: B3 and B4
  - Test result: Phase 1 + 1.5 + 1.6 + 1.7 + 1.8 + 1.9 + Phase 2 + Phase 2.1 + Phase 2.2 + Phase 2.3 + Phase 2.4 + Phase 2.5 focused suites pass — `navigationGraph.test.ts` (20) + `CampusEditor.navGraph.test.tsx` (75) + `indoorNavigationGraph.test.ts` + `FloorEditor.indoorNav.test.tsx` (16) + `editorPlacement.test.ts` + `FloorEditor.navPhase21.test.tsx` (18) + `FloorEditor.navPhase22.test.tsx` (23) + `FloorEditor.navPhase23.test.tsx` (15) + `FloorEditor.navPhase24.test.tsx` (14) + `FloorEditor.navPhase25.test.tsx` (11) + `FloorEditor.navPhase26.test.tsx` (9) + `FloorEditor.navPhase27.test.tsx` (7) + `CampusEditor.copyPaste.test.tsx` (10) + `FloorEditor.phase1_8.test.tsx` (107); Phase 2.6/2.7 verified via focused runs — map-builder suite green at 25 files / 418 tests when run without max-parallelism oversubscription (the only failure observed at 25 default workers is the previously documented B4 `AnimatePresence` floor-workflow timing test, which exceeds the 10 s `testTimeout` by ~200 ms under load and passes in isolation — no repeatable functional regression) + `navigationGraph`/`indoorNavigationGraph`/`editorPlacement` lib tests (65) green; latest `pnpm build` passed with the existing non-blocking >500 kB `AdminMapBuilderPage` Vite chunk warning; latest `git diff --check` passed clean.
  - Pull Request: Pending
- [ ] **B6 — Map Builder persistence integration**
  - Status: `BLOCKED`
  - Branch: `feature/map-builder-persistence`
  - Depends on: B2–B5 and Gate G1
  - Test result: Pending
  - Pull Request: Pending
- [ ] **B7 — Complete validation and issues workflow**
  - Status: `BLOCKED`
  - Branch: `feature/map-builder-validation`
  - Depends on: B5 and B6
  - Test result: Pending
  - Pull Request: Pending
- [ ] **B8 — Route testing and pathfinding verification**
  - Status: `BLOCKED`
  - Branch: `feature/map-builder-route-testing`
  - Depends on: B5, B7, and A5 published graph format
  - Test result: Pending
  - Pull Request: Pending
- [ ] **B9 — Save, publish, tutorial, and editor state UX**
  - Status: `BLOCKED`
  - Branch: `feature/map-builder-publish-ux`
  - Depends on: A6, B6, and B7
  - Test result: Pending
  - Pull Request: Pending
- [ ] **B10 — Authoring polish, accessibility, and performance**
  - Status: `BLOCKED`
  - Branch: `fix/map-builder-polish`
  - Depends on: B2–B9
  - Test result: Pending
  - Pull Request: Pending## Current handoff note
- B5 Phase 2.11 — Invalid Existing Edge State + Add Bend Repair UX pass (August 12, 2026): EXISTING indoor edges now have LIVE validity — every edge is evaluated against the Phase 2.10 strict thick-wall model at render (new `navEdgeIsBlocked` helper, no persisted field/migration) and an invalid edge renders RED with a small warning marker when selected; Remove Bend / Delete may intentionally leave an invalid authoring state (red, still fully editable — bend handles, segment drags and hit lines stay live so the admin can repair it) while NEW Connect creation still never commits invalid paths. Repair is interaction-driven: segment/bend drags on an ALREADY-invalid edge are allowed through blocked states, and free Waypoint/Destination movement re-derives validity live (red disappears the moment geometry clears). **Add Bend fixed (was a no-op):** a plain collinear midpoint normalized away instantly — it now splits the segment with a USEFUL orthogonal dog-leg (U-shape corner pair, modest deterministic offset, survives normalization, no duplicates), targets the hovered segment → segment after the selected bend → longest segment, and prefers the CLEAR side when one offset side is wall-blocked (rejects cleanly with feedback when both are blocked). Door openings stay valid; the same wall away from a Door stays blocked. Design Show Navigation flags invalid edges subdued red; the Issues dialog counts a wall-blocked edge and clicking it jumps to Navigation mode with the edge selected. Verification: new `FloorEditor.navPhase31.test.tsx` (11) + `navEdgeIsBlocked` lib block (4) + updated navPhase25/26 Add Bend assertions (4-bend dog-leg geometry); focused run passed; `git diff --check` ✓ clean; build ✓. Known limitations: `NavigationEdge.closed` not yet filtered by routing (B8); cross-floor links, entrance indoor↔outdoor links, route testing, student routing, accessible routes, SOS pathfinding — later B5 phases, not started. No commit made.
- B5 Phase 2.10 — Strict Wall Collision for All Connect Geometry pass (August 12, 2026): fixed the ROOT validation rule — walls are now ONE authoritative THICK-obstacle model in `indoorNavigationGraph.ts` (`segmentBlockedByWall`/`segmentWallOverlap` = centerline ± thickness/2 + clearance, exact for any orientation; `pointInsideWallObstacle`). Every Connect operation (preview, L route, detour, pinned bend, destination commit, bend drag, segment drag, Add Bend, Straighten) funnels through the SAME check — crossings, diagonal cuts, collinear overlap and wall-hugs are all caught by distance-to-obstacle; a pinned/dragged bend may NEVER settle inside a wall body or its clearance band (rejected with red feedback, Connect stays active); a wall-side anchor keeps only tiny localized contact and must leave into open floor; the Door exception is strictly LOCAL to the opening (a hug/crossing beyond it stays blocked); walking around a wall END stays legal. Preview now shows exactly what a click commits (same alignment snap + same point check) — preview validity == commit validity. Verification: `indoorNavigationGraph.test.ts` (13 new strict cases: on-thickness h/v, on-face, diagonal, bend point on wall/centerline/band, local door vs away, door-honoring point check, wall-side exit vs hug, bounds-bounded detours) + new `FloorEditor.navPhase30.test.tsx` (4 UI: wall click no-pin/no-history + Connect active, clearance-band rejection, preview==commit with no on-wall bend in the committed edge, diagonal Straighten blocked); 2 existing door tests updated to the strict LOCAL semantics; focused run **7 files / 108 tests green** (incl. navPhase25/26/28/29/29A) + nav-safety-net **6 files / 95 tests green** — the latent Phase 2.8 detour could route out of floor bounds (full-height wall), fixed via optional floor `bounds` on `orthogonalBendsFor`; `pnpm build` ✓ (existing chunk warning only); `git diff --check` ✓ clean. No commit made.
- B5 Phase 2.9A — Invalid Connect Visual Feedback pass (August 12, 2026): the Connect preview now runs the SAME wall validation as the commit — geometry that cannot be committed (wall crossing / wall-hug / clearance violation) renders RED before the click, with a red destination ring and a small "Path blocked by wall" label near the target; pinned multi-bend geometry keeps its normal styling and only the invalid proposed tail turns red. Clicking an invalid destination creates NO edge and NO history, and the connection stays ACTIVE (commit returns a success boolean; the tool only returns to Select on success) so the admin can reposition/pin a different bend — the red feedback persists until the pointer moves away. Door openings still show valid previews and commit normally. Verification: new `FloorEditor.navPhase29A.test.tsx` (5: wall-crossing preview invalid, wall-hug preview invalid, invalid click no edge/history, Connect stays active + transient feedback, door passage valid); focused run **6 files / 158 tests green**; `git diff --check` ✓ clean. No commit made.
- B5 Phase 2.9 — Navigation Scope Isolation + Wall-Clearance Routing + Single-Click Bend UX pass (August 12, 2026): focused correction from manual QA. **Scope isolation:** new `outdoorNavNodes`/`outdoorNavEdges` selectors (floorId is the indoor discriminator) are the outdoor render/read boundary — the outdoor Campus Builder, its Canvas empty-state, and TestNavigationPanel derive ONLY outdoor-scope entities while state mutations keep the full campus arrays (indoor graph never dropped); the Floor Editor already rendered only its own building+floor via `indoorNavNodes`/`indoorNavEdges` (verified: other floors + outdoor nodes stay out). **Wall-clearance routing:** walls now have effective thickness — a route must clear the centerline by `thickness/2 + WALL_ROUTE_CLEARANCE (6)` or it is rejected as a wall-hug (no more visually wall-embedded paths); a very small `WALL_ENDPOINT_TOLERANCE (4)` permits a linked anchor to touch/cross its wall at the anchor only; `orthogonalBendsFor` prefers the L whose FIRST segment leaves a wall-side anchor perpendicularly into open floor (both-safe case), detours stay clearance-validated, and Door openings remain legal passages. **Single-click bend UX:** ONE empty-space click pins the FULL previewed segment shape (orthogonal corner + click point — the click is immediately the new continuation anchor), with no-Shift alignment snapping that never wins over wall validity; temporary Ctrl+Z removes the whole last click (per-click pin-group ref), and the pinned-bend marker is now a subtle neutral outline square (distinct from Waypoint/Destination/linked cue/target ring). Verification: new `CampusEditor.scopeIsolation.test.tsx` (3) + `FloorEditor.navPhase29.test.tsx` (11: one-click L pin, preview-from-pin, preview==commit, empty-click no-op, multi-click accumulation on ONE edge, group Ctrl+Z + Escape, wall-safe preview==commit, segment-drag no growth, ramp center, violet-ring-only target, floor-scope nodes/edges) + `indoorNavigationGraph.test.ts` (7 new clearance cases); focused run **6 files / 153 tests green**; `pnpm build` ✓ (existing chunk warning only); `git diff --check` ✓ clean. Known limitations: `NavigationEdge.closed` not yet filtered by routing (B8); cross-floor links, entrance indoor↔outdoor links, route testing, student routing, accessible routes, SOS pathfinding — later B5 phases, not started. No commit made.
- B5 Phase 2.8 — Navigation Connector UX Correction + Wall-Aware Orthogonal Routing pass (August 11, 2026): focused correction based on manual QA. **Wall-aware orthogonal routing:** `orthogonalBendsFor` now validates BOTH L candidates against wall geometry (including wall-HUG overlap for a meaningful length, not just crossing) and picks the one that stays in OPEN FLOOR SPACE — a linked node on a room edge naturally leaves toward the walkable side instead of running along the wall; when both Ls are blocked it tries deterministic 2-bend U-detours and otherwise returns [] so the caller REJECTS cleanly ("Paths must pass through a door opening") instead of drawing through a wall. Preview geometry === committed geometry (pinned bend continuation also orthogonalized + wall-validated); straight (axis-aligned) still returns [] and the caller validates. **Connect empty-click fix (critical):** with NO start node selected, clicking empty floor does NOTHING — no waypoint/node/history mutation; a connection must always begin on an existing routing node. **Temporary Ctrl+Z during active Connect:** pops the LAST pinned bend while bends exist, then cancels the unfinished connection — neither creates a history entry; a committed edge remains ONE normal undo action. **Segment-drag pointer fix:** rewritten on an IMMUTABLE drag-start snapshot (original polyline + segment index) — geometry = original + current pointer delta, 1:1 pointer tracking at any zoom (no compounding, no per-pointermove bend growth); interior segments move both endpoint bends (bend count preserved), boundary segments insert AT MOST one minimal corner at drag-start topology; `normalizeBendPoints` removes duplicate/zero-length/collinear bends after every committed edit. **Alignment guides (outdoor philosophy, no Shift):** free Waypoint/Destination drags and bend drags show temporary X/Y guides + snap within an 8-unit threshold; the Phase 2.7 Shift-alignment behavior was REMOVED. **Connect-target visual simplification:** Connect mode shows ONE violet ring around the semantic node/cue and suppresses the yellow physical-target highlight for directly-targetable routing nodes; semantic colors (Waypoint green / Destination violet / linked cues / centered Ramp node) unchanged; Ramp center regression preserved. Verification: new `FloorEditor.navPhase28.test.tsx` (14: empty-click no-op, wall-hug L rejection + open-space L selection both directions, no-safe-geometry rejection, door passage valid, temp Ctrl+Z pop/cancel + committed one-action undo, 1:1 segment drag no compounding + no bend growth + interior bend-count preservation + boundary minimal corner, bend-drag snap guide + normalization, Connect target yellow suppression, Ramp center) + updated `indoorNavigationGraph.test.ts` (wall-hug/overlap, detour + clean-rejection cases, normalization, segment translation, navAlignSnap) + updated `FloorEditor.indoorNav.test.tsx` (group drag clear of snap threshold); focused run **4 files / 71 tests green**; `pnpm build` ✓ (existing chunk warning only); `git diff --check` ✓ clean. Known limitations: `NavigationEdge.closed` not yet filtered by routing (B8); cross-floor links, entrance indoor↔outdoor links, route testing, student routing, accessible routes, SOS pathfinding — later B5 phases, not started. No commit made.
- B5 Phase 2.7 — Orthogonal Connector UX + Connect Hover Fix + Centered Linked Anchors pass (August 11, 2026): focused, time-bounded UX correction before Pathway/cross-floor work. **Connect hover fix:** the actual linked routing node/cue is NEVER hidden — the violet `nav-connect-target` ring is feedback AROUND it (removed the Phase 2.6 cue suppression), and hovering directly over a routing node suppresses the physical object's yellow target highlight (node target wins). **Multi-bend Connect (draw.io-style):** empty-space clicks PIN geometry bends (`navConnectBends`, square preview markers) — no NavigationNodes, connection stays active; Escape cancels with no mutation/history; clicking a routing node commits ONE NavigationEdge carrying every pinned bend, with the final segment auto-orthogonalized exactly as previewed — all segments horizontal/vertical (no unintended diagonals); every pinned/committed segment is wall-validated. **Orthogonal segment editing:** dragging a SEGMENT of a selected segmented path translates it perpendicular (horizontal→vertical move, vertical→horizontal move) — interior segments move both adjacent bends, boundary segments move the adjacent bend + insert a corner bend so the node stays connected orthogonally; one history action per gesture. **Shift = alignment assist** for FREE node drags only (X or Y snap to the nearest routing node within 8 units; linked nodes unaffected). **Ramp centered anchor:** the ramp's routing cue/hit area now renders at the EXACT transformed center (logical node was already centered); a tiny presentation-only corner badge (`nav-linked-badge`) marks "linked" without affecting routing coordinates; the Connect target ring appears around the centered node. Straighten keeps its wall-crossing guard; all existing behavior (bend handles, selected-bend remove, multi-select, Ctrl+C/V/D, Remove/Delete, Design overlay semantics, persistence) preserved. Verification: new `FloorEditor.navPhase27.test.tsx` (7) + updated `FloorEditor.indoorNav.test.tsx` (empty-space connect now pins bends; hint text) + `FloorEditor.navPhase25.test.tsx` (ramp cue at center, badge at corner); focused run **6 files / 76 tests green**; `pnpm build` ✓ (existing chunk warning only); `git diff --check` ✓ clean. Known limitations: `NavigationEdge.closed` not yet filtered by routing (B8); cross-floor links, entrance indoor↔outdoor links (contract in Phase 2.2), route testing, student routing, accessible routes, SOS pathfinding — later B5 phases, not started. No commit made.
- B5 Phase 2.6 — Connect Feedback + Path Bend UX + Overlay Semantics + Ramp Polish pass (August 11, 2026): a SMALL focused pass (no broad refactor) addressing the remaining manual-QA interaction/visual issues before deeper routing QA. **Connect tool — clear target feedback:** a new `navNodeHover` state marks the routing node under the pointer while Connect is active — the hovered node shows ONE clean target ring (`nav-connect-target`): subtle violet dashed ring (r=9) before a start is chosen, STRONGER solid accent ring (r=12, stroke 2) once a start has been picked, so start (persistent dashed `nav-connect-start` ring) vs candidate-destination hover states are unmistakable; the cursor becomes a pointer over connectable nodes, the ring is suppressed for the start node / already-selected nodes (no stacked rings), and for linked nodes the connect-target ring REPLACES the linked cue (one state at a time). The Connect PREVIEW now shows the FINAL shape before the click — the same `orthogonalBendsFor` L-bend the commit will create (or the straight line when axis-aligned) with the endpoint snapped to the hovered node's routing coordinate (pointer-events-none). **Path bend UX:** each bend handle is now INDEPENDENTLY selectable — grabbing a handle selects that specific bend (`navSelectedBend`, selected handle renders larger/highlighted) and opens the edge inspector, so Remove Bend / Delete/Backspace removes THAT bend (any order, not reverse-creation) with neighbors reconnecting + distance recompute in ONE history action; Shift-dragging a bend constrains the changed segment strictly horizontal/vertical (dominant pointer axis), and even WITHOUT Shift a 4-unit snap tolerance aligns near-axis drags so no visually crooked 2–5° segments remain. Add Bend no longer appends onto an existing bend: it inserts the midpoint of the hovered segment (`navSegmentHover` — computed live from the pointer over the selected path's hit line) or, without a hover, of the LONGEST segment, inserting at the correct polyline position for multi-bend paths. Straighten is now WALL-SAFE: it is disabled (with guidance "The direct line crosses a wall — keep a bend or add a door opening.") and aborts via a warning toast whenever the direct A→B line would cross a wall without a door — never silently creating an invalid route. **Design overlay semantic colors:** the read-only Show Navigation overlay no longer renders everything green — it preserves the mode's semantics with subdued styling (Waypoint = green, Destination = violet, linked locations = green linked-cue, edges = route color at reduced opacity) so the reference view still communicates what each element is. **Ramp visual + anchor:** `RampSymbol` is now a recognizable accessibility sign — deep-blue (`#2563eb`) rectangular footprint, clean border, a significantly LARGER centered white wheelchair icon (0.68 of min dimension, ≥10 units) as the dominant visual, and only a tiny corner Up/Down/Both cue (bottom-right, never competing); the ramp's LOGICAL routing anchor moved to the object CENTER (route edges terminate exactly there — `resolveIndoorLinkedPosition` + creation + sync all agree) while the small visual "linked" badge renders offset at a top-right interior cue position (`rampLinkedCuePosition` repurposed) so routing geometry stays centered but the badge never covers the accessibility icon. All prior behavior preserved (segmented persistence/polyline distance/per-segment wall validation, Waypoint/Destination/Link Location/Connect workflow, multi-select, Ctrl+C/V/D, Remove/Delete, pan/zoom/Fit, Room routing anchor, circulation sync, wall snapping, Design↔Navigation transition, outdoor navigation). Verification: new `FloorEditor.navPhase26.test.tsx` (9: pre-start vs post-start Connect target rings + start ring, Shift-constrained bend drag stays axis-aligned, Add Bend splits the longest segment without stacking on an existing bend, Remove Bend removes the SELECTED inner bend with neighbors reconnecting + distance recompute, Straighten blocked (disabled, no mutation) when the direct line crosses a wall vs succeeds when wall-safe, Design overlay preserves Waypoint green vs Destination violet semantics, ramp routing anchor at center + edge endpoint lands there while the badge offsets, ramp deep-blue accessibility-sign visual with larger white icon + corner cue) + updated `FloorEditor.navPhase24.test.tsx` (ramp anchor 182,26 + corner cue) + `FloorEditor.navPhase25.test.tsx` (center-anchor/badge split) + `indoorNavigationGraph.test.ts` (ramp resolver center). Map-builder suite: 25 files / 418 tests green (with reduced workers; the only failure at 25 default workers is the previously documented B4 `AnimatePresence` floor-workflow timing test — it passes in isolation at ~3.6 s and exceeds the 10 s `testTimeout` by ~200 ms only under suite-level parallel contention; no repeatable functional regression). `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning; `git diff --check` clean. Known limitations: `NavigationEdge.closed` is stored/editable but not yet filtered by routing (B8); cross-floor stairs/elevator links, entrance indoor↔outdoor links (contract documented in Phase 2.2), route testing, student routing, accessible route computation, and SOS pathfinding remain later B5 phases — not started. No commit made.
- B5 Phase 2.5 — Segmented Paths + Design Nav Overlay + Ramp Visual pass (August 11, 2026): kept narrow and fast per spec. **Segmented / orthogonal navigation paths:** `NavigationEdge` gained optional `bendPoints?: {x,y}[]` (pure GEOMETRY — routing endpoints stay `startNodeId`/`endNodeId`; persisted through the edge's existing metadata JSON, no DB migration; legacy straight edges untouched). Connect now auto-creates a wall-safe orthogonal L-bend for non-aligned indoor nodes (`orthogonalBendsFor` prefers horizontal-then-vertical, falls back to vertical-then-horizontal; axis-aligned nodes stay straight) and the distance/weight is the TOTAL polyline length (`navEdgePolylineDistance` — start → bends → end), never the straight diagonal. Wall-crossing safety validates EVERY segment (`edgePolylineCrossesWallWithoutDoor`): a segmented path routing AROUND a wall corner is allowed, any segment crossing a solid wall without a door opening is rejected with the existing "Paths must pass through a door opening" guidance (both L candidates + the straight fallback are checked before commit). **Bend editing:** selected segmented paths render small bend handles (`nav-bend-handle`, Select tool only) — drag reshapes live with ONE history action on release (same `suppressHistory`/`navSnapshot` pattern as node drags; Ctrl+Z restores in one step); the Navigation Path properties panel gained a **Path Shape** section — Add Bend (polyline midpoint), Remove Bend, Straighten Path — each one history action; straightening restores the direct distance. **Clean geometry:** rendered edges are polylines (stroke joins rounded), the one-way direction arrow anchors at the polyline midpoint oriented along the containing segment, and all geometry favors horizontal/vertical segments. **Node/linked-owner movement:** endpoints always resolve from the CURRENT node coordinates; authored bends stay stable (absolute); `commitNavGraph` recomputes segmented-edge distance after every commit so node/bend edits stay accurate; Ctrl+C/V and Ctrl+D preserve bend geometry — bends now TRANSLATE WITH the pasted nodes (the copied L-shape stays attached to its pasted endpoints) with fresh edge IDs and polyline-distance recompute (a paste-distance bug that used straight-line hypot was fixed); floor duplication already carries bends via the existing `remapIndoorNavForFloorCopy` spread (identical copy geometry), and `normalizeNavGraph` coerces bend coordinates on hydrate. **Design-mode Show Navigation overlay:** a compact "Show Navigation" toggle in the Design header renders the graph as a READ-ONLY reference overlay (`floor-nav-overlay`) — reduced opacity, thinner lines, smaller dots, linked cues included — with `pointer-events-none` so Design Select still edits ONLY floor objects (graph is never selectable/movable/dirty in Design); it is a pure view preference (no dirty, no history, camera preserved) and hidden in Navigation mode. **Ramp visual simplification:** `RampSymbol` is now a functional BLUE footprint with a CENTERED lucide `Accessibility` wheelchair icon as the primary visual (`ramp-accessibility-icon`) plus only a small secondary Up/Down/Both direction cue (`ramp-direction-cue`) when the object is large enough — the complex rail/arrow drawing is gone; local-frame so rotation/resize stay centered. **Ramp nav cue:** the ramp-linked NavigationNode now uses a deterministic interior anchor (`rampLinkedCuePosition` — top-right inside the footprint, mirroring the room routing-anchor philosophy) so the small connected cue and edge endpoints never cover the centered wheelchair icon; `resolveIndoorLinkedPosition` + ramp node creation + sync all agree. Preserved and regression-tested: Waypoint/Destination/Link Location/Connect workflow, selection/drag, multi-select, copy/paste/duplicate, Remove/Delete, Room routing anchor, Room/Door/Stair/Elevator/Ramp linking, wall snapping, Design↔Navigation transition, pan/zoom/Fit, and outdoor navigation (outdoor editor shares only `normalizeNavGraph`, which is bend-safe). Verification: new `FloorEditor.navPhase25.test.tsx` (11: orthogonal auto-bend + polyline distance 130 vs diagonal 94, axis-aligned straight, full-wall rejection across segments, around-the-corner L allowed, Path Shape Add/Remove/Straighten one-action each, bend-handle drag + single-undo restore, Ctrl+C/V bend preservation with fresh IDs + translated bends + polyline distance, Design overlay read-only + never dirties + no hit surfaces + hidden in Navigation, blue Ramp icon centered, ramp-linked cue at the interior anchor) + updated `FloorEditor.navPhase24.test.tsx` (polyline endpoint assertions + ramp blue-symbol assertions) + `FloorEditor.indoorNav.test.tsx` (polyline endpoint) + `FloorEditor.navPhase23.test.tsx` / `FloorEditor.phase1_8.test.tsx` (ramp symbol assertions) + `indoorNavigationGraph.test.ts` (ramp anchor 154,84); focused map-builder run passed 24 files / 409 tests (only the known B4 `AnimatePresence` floor-workflow timing flake appeared under full-suite load and passed in isolation); `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning; `git diff --check` clean. Known limitations: `NavigationEdge.closed` is stored/editable but not yet filtered by routing (B8); cross-floor stairs/elevator links, entrance indoor↔outdoor links (contract documented in Phase 2.2), route testing, student routing, accessible route computation, and SOS pathfinding remain later B5 phases — not started. No commit made.
- B5 Phase 2.4 — Indoor Navigation Tool Simplification + Route Anchor + Circulation Visual Corrections pass (August 11, 2026): finished the single-floor Navigation workflow so it can be frozen before outdoor Pathway snapping and cross-floor linking. **Wall creation snap regression — real-browser hit-testing root cause + fix:** the jsdom reproduction completed walls in ONE click, so the manual "multiple clicks to connect to a Room-attached Wall" bug was a real-browser event-interception defect: the transient wall-drawing preview `<g>` (a 4px-stroke line whose endpoint sits exactly under the cursor) rendered ABOVE the walls WITHOUT `pointer-events-none`, so the completion click hit the preview line instead of reaching the svg background handler — and the room/stairs drag preview and drawing-path groups had the same hazard. All three transient preview groups are now `pointer-events-none` (the wall-snap indicator already was), so the wall completion click always lands on the canvas; the jsdom regression tests assert the preview group's `pointer-events-none` class and one-click commit against a Room-attached wall endpoint + segment (T-junction), plus free-wall behavior unchanged. **Floor Navigation tool architecture simplified to ONE clear workflow:** the top toolbar is now Select / Pan / Connect / Remove — the separate top-toolbar Add Waypoint action is GONE (waypoint creation lives in the library) and "Connect Path" is renamed **Connect** (tooltip "Connect routing points and locations"). The left Navigation Library remains the only way to intentionally create/link routing nodes: CREATE → Waypoint (technical bend/intersection) + Destination (free named routable point), LINK → Link Location (infers the physical type from the actual Room/Door/Stair/Elevator/Ramp you click; empty space is rejected with "Nothing to link / Select an existing floor location to link." — no generic waypoint is ever silently created). Free Waypoint/Destination placement on semantic linked-location targets stays blocked with Link Location guidance (Phase 2.3 rule preserved, including large-room interiors). **Room route-anchor correction (routing geometry NEVER derives from the label):** manual QA showed edges appearing to terminate toward the Room TEXT — because room-linked nodes sat at the room CENTER (where the label renders) while the visual cue was offset. The canonical room-linked NavigationNode position is now the deterministic routing anchor itself (`resolveIndoorLinkedPosition` room case + `navWaypointAt` creation both use `roomLinkedCuePosition(room)` — a stable offset above the center, rotated with the room): the nav cue renders centered on the node, NavigationEdges terminate EXACTLY at the node coordinate, and the room name stays a presentation-only label at center. Existing persisted room nodes migrate automatically because `syncIndoorLinkedNodePositions` recomputes linked positions on hydrate. **Circulation visual finalization (all local-frame → rotation/resize-safe):** StairsSymbol now adds subtle boundary guard rails (`stairs-rail`, both footprint edges) alongside the treads + centered dashed run line + directional arrow (Up / Down / Both follow the existing metadata; properties use the compact SegmentControl); ElevatorSymbol's up/down chevrons are now a stacked vertical ▲ over ▼ pair centered SYMMETRICALLY in the cab (scaled to cab size, stays centered after resize/rotation); RampSymbol keeps its footprint + parallel rails + centered slope-direction arrow and gains a SMALL hand-drawn wheelchair accessibility glyph (`ramp-accessibility`) — an original vector, no external/copyrighted asset — tucked inside the footprint corner so the ramp still reads as a physical ramp. **Circulation linked-node anchors verified centered:** Stair/Elevator/Ramp linked nodes sit at the transformed owner center and edges terminate exactly at the node (regression tests assert node data + rendered edge endpoints). All Phase 2.3 multi-selection / Remove / Copy-Paste / Pan-Zoom-Fit / mode-transition behavior is preserved — regression-tested, not reworked. Verification passed: new `FloorEditor.navPhase24.test.tsx` (14: one-click wall completion on Room-attached wall endpoint + T-junction, pointer-events-none drawing previews, simplified toolbar shape with NO top Add Waypoint, library = Waypoint/Destination/Link Location only, Link Location empty-space rejection with no mutation, free Waypoint/Destination placement, room edge endpoint == node anchor (never label center), stair treads/rails/centered arrow + Up vs Down paths, elevator shaft/cab/door + chevrons centered before AND after resize, ramp rails/arrow/accessibility cue inside footprint, circulation linked anchors + edge endpoints) + updated `FloorEditor.indoorNav.test.tsx` / `FloorEditor.navPhase21.test.tsx` / `FloorEditor.navPhase22.test.tsx` / `FloorEditor.navPhase23.test.tsx` (toolbar Connect rename + library-arm waypoint) + updated `indoorNavigationGraph.test.ts` (room anchor assertions). Full `pnpm test`: two concurrent runs showed intermittent load-induced timing flakes (6 then 5 failures) and sequential runs showed 1–3 failures, ALL in untouched B4 floor-tab/menu/AnimatePresence/viewport timing tests (`reorders floors`, `moves the active floor`, `opens the shared menu from a tab right-click`, `auto-fits the floor viewport`); every one passed in isolation, and the final clean-machine sequential run passed **62 files / 815 tests** with zero failures (this matches the previously documented intermittent parallel-load flake class — no repeatable regression). `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning; `git diff --check` clean. Known limitations: `NavigationEdge.closed` is stored/editable but not yet filtered by routing (B8); cross-floor stairs/elevator links, entrance indoor↔outdoor links (contract documented in Phase 2.2), route testing, student routing, accessible route computation, and SOS pathfinding remain later B5 phases — not started. No commit made.
- B5 Phase 2.3 — Indoor Navigation Multi-Select + Placement Rules + Circulation Visual Polish pass (August 11, 2026): finished the indoor Navigation interaction foundation and improved circulation visuals. **Multi-selection now PERSISTS on member click/drag (BLOCKING fix):** the proven Figma/outdoor pattern from Floor Design is now applied to indoor nav nodes — clicking or dragging a member of the current multi-selection preserves the whole group (never collapses to single), so the multi-selection Properties summary stays open and a drag moves ALL selected free waypoints rigidly (relative positions preserved, edges follow derived node positions, ONE history action on release — a single Ctrl+Z restores the whole group); only clicking an UNRELATED node collapses to single-object editing; Shift-click toggling and marquee selection are unchanged. **Mixed-selection movement rule (predictable):** dragging a free member of a selection that also contains linked locations moves the free nodes while linked nodes stay attached to their physical owners, with one concise toast ("Linked locations stay put"). **Free placement rules:** the Waypoint / Destination tools now place FREE nodes only on open walkable floor space — clicking directly on a linked-location semantic target (Door / Stair / Elevator / Ramp, or the Room center label zone) is rejected with "Use Link Location for this object" guidance and NO mutation/history; the interior of a large room away from its center zone remains valid free space (deliberate, so admins can route through large rooms without blanket-bans); the same rule applies to Navigation Library drag-and-drop, which now shows a red ✕ not-allowed preview (new `navDragBlocked` state, cleared on drop/dragend/mode-switch) instead of a green ghost and rejects the drop cleanly; the free tools also select an existing node under the pointer instead of duplicating it. **Room linked-cue label occlusion fixed:** the room-linked navigation cue now renders at a stable offset position (new pure `roomLinkedCuePosition(room)` — above the room center in room-local space, rotated with the room so it follows moves/resizes/rotations predictably) instead of sitting on top of the room's own name label; the room name stays the primary label, the cue remains subtle, and the hit circle / selection ring / erase ring all follow the cue. **Circulation visual redesigns (all local-frame so they rotate/resize with the object):** `StairsSymbol` now draws repeated horizontal tread lines (`stairs-tread`) + a centered dashed run line + a directional arrow (`stairs-arrow`, up/down/double for direction metadata) clearly communicating ascent/descent; `ElevatorSymbol` draws an outer shaft frame + inner cab + a horizontally-centered door opening + SYMMETRIC up/down chevrons centered in the cab (`elevator-shaft`/`elevator-cab`/`elevator-door`/`elevator-chevrons`) — fixing the old off-center chevron path that drifted after resize; `RampSymbol` draws a rectangular footprint + parallel rail cues along the travel axis (`ramp-rail`) + a centered directional arrow (`ramp-arrow`, slope-width aware, up/down/both) + level end ticks — replacing the old abstract off-center diagonal + dot. All the legacy `opacity={0}` dead decorations (slope lines, ♿/↑↓↕/E texts, overlay rects) were removed from the circulation render blocks, and the stairs/ramp Direction property in the Design Properties panel now uses the existing compact `SegmentControl` (Up/Down/Both) instead of native selects. **Remove tool regression:** the Remove tool now removes ONLY the targeted node/edge (consistent with Floor Design erase) — it previously merged the current multi-selection into the deletion, so erasing one group member deleted the whole group; after a targeted remove the remaining multi-selection stays selected (only the erased id is pruned), while the Delete key still removes the full selection. **Copy/paste/duplicate regression:** pasting or duplicating a multi-selected free-node group keeps the pasted set multi-selected (summary inspector stays open), preserves relative positions, remaps edges to the duplicated nodes (both endpoints must be copied), and stays one history action. Verification passed: new `FloorEditor.navPhase23.test.tsx` (15: pure placement-block helper + cue-offset helper, Add Waypoint on Door/Room-center blocked with guidance + no mutation, room-interior free placement allowed, Destination blocked, drag-drop blocked preview + rejected drop, multi-selection preserved on member click + unrelated-click collapse, rigid group drag in one undo, mixed-selection toast rule, Ctrl+C/V/D group with remapped edges staying multi-selected, Remove tool targeted-only with remaining selection preserved, stair treads/arrow + centered elevator chevrons + ramp rails/arrow + rotation wrappers + small linked cues) + updated `FloorEditor.indoorNav.test.tsx` / `FloorEditor.navPhase21.test.tsx` (room/door/stair linking now via Link Location, since the free tools reject linked-location targets) + updated `FloorEditor.phase1_8.test.tsx` ramp-symbol assertion for the redesigned symbol; full `pnpm test` run 1 passed 61 files / 801 tests and run 2 passed 61 files / 801 tests (both green — no flakes); `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning; `git diff --check` clean. Known limitations: `NavigationEdge.closed` is stored/editable but not yet filtered by routing (B8); cross-floor stairs/elevator links, entrance indoor↔outdoor links, route testing, student routing, accessible route computation, and SOS pathfinding remain later B5 phases — not started. No commit made.
- B5 Phase 2.2 — Indoor Navigation UX Simplification + Interaction Fixes pass (August 11, 2026): manual browser QA of Floor Navigation found real interaction defects; all fixed. **BLOCKING click-selection root cause fixed:** indoor nav nodes rendered ALL children as `pointer-events-none`, so the `<g>` never received pointer events in real browsers — click-select and direct drag never fired (marquee worked only because it is JS hit-testing, which is why tests missed it). Every indoor nav node (free + linked) now renders a transparent invisible hit circle (`data-testid="nav-node-hit"`, r=11) as the actual click/drag surface, so a free Waypoint selects by direct click and drags directly (edges follow derived node positions, one history action on release), at any zoom level; the edge hit line (transparent stroke) already used the same pattern. **Design ↔ Navigation mode transition:** the canvas NEVER remounts or resets — zoom/pan/floor/graph are preserved; the toolbar, left sidebar content, and nav Properties panels now fade/slide subtly on switch via keyed `motion.div` wrappers (~180ms, canvas untouched); entering Navigation still never auto-opens Properties. **Waypoint vs Destination are now genuinely distinct:** Destination is created from the library (drag onto floor or click to arm the Destination tool, `room_access` type) and renders as a compact violet target/pin instead of the plain green dot; its inspector heading is DESTINATION (Name strongly encouraged, fixed Destination type, no raw type fiddling) vs WAYPOINT (optional Name, type selector) vs linked `{Room/Door/…} Point`. **Navigation Library simplified:** the five per-kind linked items (Room Destination / Door Connection / Stairs / Elevator / Ramp) are replaced by ONE unified **Link Location** action — click it, hover any Room/Door/Stair/Elevator/Ramp (target highlights), and click to create/reuse the correct canonical linked node inferred from the actual object; wrong-type drops are now impossible by construction (no per-kind drag items remain; the drop/dragover handlers only support free Waypoint/Destination), invalid clicks mutate nothing with concise guidance, and Escape returns to Select. **Multi-selection inspector:** selecting 2+ nav nodes opens SELECTED NAVIGATION OBJECTS (Waypoints / Destinations / Linked Locations / Paths counts, Delete Selected, Clear Selection) instead of showing the first node's single inspector; group drag moves free waypoints rigidly while linked locations stay attached, with one concise toast when the selection was mixed. **Remove tool:** destructive hover now shows a red dashed ring on the hovered node/edge (`nav-erase-hover`), node/edge cursors are `not-allowed` while Remove is active (they previously overrode the canvas cursor with grab/pointer, making Remove look like Select), clicking removes the graph element via the existing cleanup (one history action) and NEVER touches the physical Room/Door/Stair/etc.; Delete/Backspace + input guards unchanged. **Cross-editor Remove/Erase cursor:** the Floor Navigation node/edge cursors now respect the erase tool, and the Canvas decor asset cursor + erase click path were fixed (`tool === "select" ? "move" : cursor`, erase opens the delete-confirm) so Outdoor Campus Erase, Outdoor Navigation Remove, Floor Design Erase, and Floor Navigation Remove all show `not-allowed` over the canvas. **Real bug fixed in `mergeFloorNavIntoCampus` (Phase 2 regression):** the old edge merge kept every campus edge whose id was NOT in the new set — so deleting an edge left the stale edge in `campus.navEdges` (with dangling endpoint references) even though the DOM renderer hid it; the node-delete test passed only because the renderer drops edges with missing endpoints. The merge now drops all same-floor-owned edges (both endpoints indoor on this floor) and appends `nextEdges`, so edge/node deletion truly removes them from campus data while indoor↔outdoor entrance links are preserved. Also fixed a stale-closure bug: the window keydown listener's Escape branch reads `navTool` but `navTool` was missing from its dependency array (Escape couldn't cancel an armed Link Location/Destination tool); the multi-panel Paths count now matches exactly what Delete Selected removes (edges touching any selected node). Verification passed: new `FloorEditor.navPhase22.test.tsx` (23: mode transition preserves canvas+camera, no auto-opened sidebar, direct node click selection + hit targets under zoom, node drag + edges follow + one-history undo, Waypoint vs Destination visuals/headings, Link Location infers Room/Door/Stair/Elevator/Ramp + reuse + invalid-target no-op + Escape cancel, multi summary not first-node inspector + mixed group-drag rule, Remove node/edge + destructive hover + not-allowed cursor + graph-only (never falls through to Design deletion), cursor parity across tools, Delete + Ctrl+C/V/D + input guards, view-only no-dirty pan/zoom/fit) + updated `FloorEditor.navPhase21.test.tsx` (linked drag items → Link Location clicks) + `CampusEditor.copyPaste.test.tsx` (10, incl. cross-editor Erase/Remove cursors); full `pnpm test` run 1 AND run 2 both passed 60 files / 786 tests (no flakes); `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning; `git diff --check` clean. **Outdoor↔indoor entrance contract (documented, not implemented):** outdoor Building Entrances and indoor floor coordinates are DIFFERENT coordinate spaces, so NO indoor entrance node is auto-placed at a guessed x/y. The next-phase contract: Outdoor Navigation keeps the canonical Building Entrance node (buildingId + entranceId); Floor Navigation will surface "Entrance needs indoor link" and the admin picks the actual Door on the appropriate floor (suggesting the Building's configured default/ground floor as a hint only, never as permission to guess coordinates) → Outdoor Entrance ↔ logical entrance connector ↔ indoor Door-linked NavigationNode becomes the indoor/outdoor transition. The Navigation Library intentionally shows no fake "Needs linking" entrance section yet. Known limitations: `NavigationEdge.closed` is stored/editable but not yet filtered by routing (B8); cross-floor stairs/elevator links, entrance indoor↔outdoor links, route testing, student routing, accessible route computation, and SOS pathfinding remain later B5 phases — not started. No commit made.
- B5 Phase 2.1 — Floor Editor Navigation UX / Shortcuts / Tooling Correction pass (August 10, 2026): made Floor Editor Navigation mode feel as polished as Outdoor Map Builder and Floor Design before deeper indoor-routing QA. **Copy / paste / duplicate shortcuts (all four editors):** Ctrl+C copies the current supported selection into an editor-local clipboard (outdoor: buildings/markers/decor/paths; outdoor Navigation: free waypoints + edges; Floor Design: rooms/walls/doors/windows/fpaths; Floor Navigation: free waypoints + edges), Ctrl+V pastes a duplicate with fresh IDs (never original IDs) + a small +25/+25 world-unit offset (increments per paste, clamped to canvas/floor bounds) and selects the pasted result as ONE history action, and Ctrl+D duplicates the selection immediately with the same remap rules — all with an editable-target guard (INPUT/TEXTAREA/SELECT/contenteditable) so native text copy/paste is never hijacked. **Relationship remaps:** building duplicates regenerate entrance IDs + remap to the new building (`normalizeBuildingEntrances`), decor uses `duplicateDecorAsset`, pasted nav edges remap endpoints to the duplicated nodes (edges only copied when BOTH endpoints were copied), and entrance-linked/room/door/circulation-linked nodes are NEVER duplicated as a second canonical link to the same physical object — they are excluded from the copy set with clear feedback ("Entrance waypoints not copied"/"Entrance waypoints excluded"). **Floor Navigation view controls:** switching Design ↔ Navigation preserves zoom/pan exactly (no reset, no dirty), Navigation Pan now actually pans (the shared `movePan` is wired into the Navigation branch — previously pan was inert in Navigation mode), Ctrl+wheel zoom, Zoom In/Out, Fit Floor, and the zoom % control all remain identical to Design, and view-only actions never mark the draft dirty. **Properties sidebar:** entering Navigation no longer auto-opens the verbose "Build the Walking Network" tutorial panel — the right sidebar stays CLOSED with nothing selected and primarily represents the CURRENT SELECTION (waypoint → Waypoint Properties, path → Navigation Path Properties, linked node → linked properties, multi → selected summary); node/edge creation paths explicitly open the inspector on the created item. The old verbose instructional panel is replaced by a restrained canvas empty-state overlay (shown only when the floor has no nav graph) with an Add Waypoint action and a compact non-intrusive sidebar hint. **Navigation Library left sidebar:** the Floor Design Object Library is replaced in Navigation mode by a dedicated NAVIGATION LIBRARY (Waypoint + Destination under WAYPOINTS; Room Destination / Door Connection / Stairs / Elevator / Ramp under LINKED LOCATIONS with "click/drop onto an existing …" guidance) — no Chair/Table/Wall placement assets are visible or draggable in Navigation. **Drag-and-drop authoring:** dragging Waypoint from the library shows a ghost placement preview and drops a canonical indoor `createIndoorNavNode` at the exact floor world coordinate (works under zoom/pan/floor bounds, never 0,0), selects it, opens Properties, and returns to Select; dragging a linked item (Room/Door/Stairs/Elevator/Ramp) highlights the matching physical object on hover and creates/reuses the canonical linked node on drop (never a fake physical clone), with clean rejection (no orphan node) on invalid space. **Select/marquee parity:** Navigation marquee rubber-band selects ONLY graph elements (circle-rect node hit + segment-rect edge hit), Shift-click toggles, and group drag moves free waypoints as one rigid group while linked nodes stay derived. Connect Path remains available alongside the library for rapid drawing. Verification passed: focused `FloorEditor.navPhase21.test.tsx` (18: shortcuts incl. input guards + one-history undo, camera preservation + real Navigation pan, Ctrl+wheel zoom + no-dirty view actions, sidebar closed on entry + Navigation Library vs Object Library, drag/drop waypoint exact placement under zoom/pan, room/door/stair/elevator/ramp linked drops + reuse + invalid-drop rejection, marquee graph-only + group drag) + `CampusEditor.copyPaste.test.tsx` (8: building copy/paste + entrance remap + undo, Ctrl+D building, marker copy/paste offset, input guard, empty-clipboard info, nav waypoint copy/paste with edge endpoint remap, nav Ctrl+D, entrance-linked exclusion) + all existing map-builder suites re-run green (204 focused + 344 map-builder); full `pnpm test` run 1 passed 59 files / 761 tests, run 2 passed 760/761 with the single failure passing in isolation (known parallel-load flakiness); `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning; `git diff --check` clean. Known limitations: `NavigationEdge.closed` is stored/editable but not yet filtered by routing (B8); cross-floor stairs/elevator links, entrance indoor↔outdoor links, route testing, student routing, accessible route computation, and SOS pathfinding remain later B5 phases — not started. No commit made.
- B5 Phase 2 — Indoor Navigation Authoring Foundation pass (August 10, 2026): brought the SAME canonical NavigationNode / NavigationEdge authoring system into the Floor Editor for reliable SINGLE-FLOOR indoor routing. **Design | Navigation mode switch:** `FloorEditorMode` now includes `"navigation"`; a compact Design/Navigation mode switch keeps B4 architectural authoring in Design and shows a focused graph toolbar (Select / Pan / Add Waypoint / Connect Path / Remove, tooltips + aria-labels matching outdoor Navigation) in Navigation, where architectural tools are inert and rooms/walls/doors/furniture/circulation remain visible context-only. **Indoor Select is graph-only:** single click, Shift multi-select, and marquee select indoor nodes/edges on the current floor; multi-selected free waypoints drag as one rigid group (one history gesture) with edges derived from endpoints (never independently draggable); linked physical objects cannot be moved in Navigation mode. **Indoor waypoints:** Add Waypoint creates a canonical NavigationNode with campusId/buildingId/floorId + floor world coordinates via new `createIndoorNavNode` (outdoor `createNavNode` + linked refs), selects it, opens Waypoint properties, returns to Select. **Connect Path (indoor):** same interaction as outdoor — waypoint → waypoint, waypoint → empty floor location, empty → empty, with a ghost preview node over empty space; Escape/right-click/tool switch cancel cleanly with no orphan nodes/edges. **Room destinations:** clicking a Room creates/reuses ONE canonical room destination node (roomId/buildingId/floorId, room-center position) with a doorway-aware guard — a room with no Door shows "Add a door before connecting this room to the walking network." instead of silently creating an unusable route through the wall. **Door-linked nodes:** clicking a Door creates/reuses its canonical Door node (doorId + resolved door world coordinate); it follows the Door along the wall, wall movement, room-driven anchored wall movement, and wall rotation, and is not freely draggable. **Stairs/Elevator/Ramp:** floor-local linked nodes derive position from the object center and follow it when moved/resized; accessibility defaults Stairs=No, Elevator=Yes, Ramp=Yes (editable). **Wall-crossing safety:** pure `edgeCrossesWallWithoutDoor` rejects edges that cross a wall without passing through/terminating at a Door opening ("Paths must pass through a door opening.") — no full topology. **Linked-node synchronization + orphan cleanup:** pure `syncIndoorLinkedNodePositions` keeps linked node positions derived from their physical owner on every relevant mutation; `pruneOrphanedIndoorNodes` removes linked nodes + connected edges when their Room/Door/Stairs/Elevator/Ramp is deleted in the SAME meaningful history action (undo/redo restores). **Floor duplication:** `remapIndoorNavForFloorCopy` gives the duplicated floor fresh nav node/edge IDs with floorId and roomId/doorId/stairs/elevator/ramp linked refs remapped to the duplicated objects — no references back to the original floor. **Properties:** new `FloorNavPropertiesPanel` reuses outdoor language — Waypoint (Name, Type, Accessible, Connections, compact Position) and Navigation Path (From/To, Direction, Accessible, Emergency Safe, Closed, distance in world units), custom NaviSync controls, linked nodes show human-readable Type + linked object + read-only derived Position. **History/dirty:** graph mutations are one history action and mark the shared Campus draft dirty (outer Save enables) via an `indoorNavKey` folded into the floor dirty key (baseline set correctly on save/discard/floor switch); selection/hover/mode-switch never mark dirty; physical-owner moves create no extra graph history. **Persistence:** indoor nodes/edges persist through the existing navigation_nodes/navigation_edges services with buildingId/floorId + linked metadata; `campusStructureService.ts` hydrate synchronizes linked node positions and drops only dangling same-floor edge references (indoor↔outdoor entrance links preserved); a `buildFloorUpdates`/`normalizeFloor` fix strips nav keys so they never leak into FloorPlan objects (which previously left the outer campus permanently dirty after edit+undo). Verification passed: focused `indoorNavigationGraph.test.ts` + `FloorEditor.indoorNav.test.tsx` (16) + indoor floor-duplication remap integration, full `pnpm test` (57 files / 735 tests), `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning, and `git diff --check` clean. Known limitations: cross-floor stairs/elevator links, entrance indoor↔outdoor links, route testing, student routing, accessible route computation, and SOS pathfinding remain later B5 phases — not started; `NavigationEdge.closed` is stored/editable but not yet filtered by routing (B8). No commit made.
- B5 Phase 1.9 — Entrance Navigation Visual Cleanup pass (August 10, 2026): a small visual-only correction — entrance-linked navigation is functionally correct but manual QA showed multiple stacked/concentric circles during hover/select/connect that took too long to settle. **One indicator at a time:** entrance-linked NavigationNodes now render exactly ONE navigation-state decoration with strict priority — Connect Target > Connect Start/Destination > Selected > Connected (badge) — implemented as a single mutually-exclusive render branch in `Canvas.tsx`; the physical door symbol stays the primary visual. **Normal state:** tiny connected badge only (r=4 dot + one subtle r=6.5 ring that doubles as the single hover highlight, ~150ms), no separate always-on hover ring, no stacked waypoint target. **Selected:** ONE clean accent ring sized to the entrance icon (r=12), badge suppressed underneath, no pulsing. **Hover:** the badge ring is the single subtle highlight; it is never combined with selected/connect rings. **Connect Target:** hovering an entrance with Add Waypoint / Connect Path now shows ONLY the entrance overlay's single dashed target ring (`data-testid="entrance-connect-target"`, r=17) — the linked nav-node renders nothing extra while it is active, and the ghost waypoint preview (`nav-path-preview` node circles) is suppressed over the entrance so no circles stack (the dashed edge preview line is kept); moving the pointer away clears the target ring and restores the badge immediately (no lingering decoration; transitions are 150ms, within the recommended 100–180ms). **Label:** entrance-linked nodes keep the entrance name (e.g. "Primary Entrance"), never a generic "Waypoint" label, and it is not duplicated on hover/select. **Edge endpoint:** connected edges terminate centered on the physical entrance. **Unchanged:** normal free outdoor Waypoints keep their node circle + hover/selected rings; all Phase 1.8 behavior (node follows building, drag blocking, dedup, delete cleanup, properties, dirty/history, persistence) is preserved. Verification passed: focused `CampusEditor.navGraph.test.tsx` (75 tests — new Phase 1.9 regressions cover single normal cue, single selected ring + no generic label, connect-target priority + immediate clear on pointer leave, unchanged plain waypoint visuals, edge endpoint + transient cleanup), full `pnpm test` (55 files / 701 tests), `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning, and `git diff --check` clean (CRLF normalization warnings only). Known limitations: `NavigationEdge.closed` is stored/editable but not yet filtered by `findNavigationRoute` (route filtering arrives with B8); indoor Floor Editor graph authoring, cross-floor stairs/elevator links, entrance indoor↔outdoor links, route testing, student routing, and SOS pathfinding remain later B5 phases — not started. No commit made.
- B5 Phase 1.8 — Entrance-Linked Navigation Final Corrections pass (August 10, 2026): entrance-linked NavigationNodes are now position-synchronized with their B3 building entrance — the physical Entrance is the one source of position truth. **Position synchronization:** pure `syncEntranceNodePositions(buildings, navNodes)` (in `navigationGraph.ts`) recomputes each entrance-linked node's x/y from the resolved entrance world position and is applied on every building move/resize/rotate, group drag, entrance perimeter drag, entrance property update, and on hydrate (`campusStructureService.ts` derives node positions after loading), so Building move → Entrance moves → linked Node moves → connected NavigationEdges follow automatically with no manual repair and no extra history step (the building gesture remains the single authored action; undo/redo restores the full relationship). **Linked-node drag blocking:** entrance-linked nodes are selectable/inspectable/connectable but NOT independently draggable — a drag on the node selects it and shows "Entrance waypoints follow their building entrance." without arming movement, creating no history/dirty change; in mixed multi-selection moves, linked entrance nodes are excluded from translation while free waypoints still move (least-surprising behavior). **Duplicate feedback:** Add Waypoint on an entrance that already has its canonical node now selects the existing node, opens its properties, and shows an info toast "Entrance already connected to the navigation network." with no mutation; Connect Path reuses the existing entrance node and a duplicate edge is rejected with the shared "Those points are already connected" toast. **Entrance visual simplification:** entrance-linked nodes no longer render the normal waypoint target — the physical entrance symbol stays and a single small connected-badge ring (subtle, ≤6.5px) marks navigation membership; hover/selected/connect-target states show one clean ring at a time; the entrance name is used as the display label with no stacked generic "Waypoint" label. **Type lock:** entrance-linked nodes keep Type locked to Building Entrance with the helper "Linked to building entrance." (unchanged from 1.7). **Entrance/building delete cleanup:** deleting a B3 entrance or its building now prunes the orphaned entrance-linked node + its connected edges in the SAME history action via pure `pruneOrphanedEntranceNodes` (also applied in batch building delete and keyboard Delete) — no stale `entranceId` references; undo/redo restores/removes the relationship consistently. **UI:** a shared `BoolSegmented` control (comfortable h-9, balanced-width Yes/No, Open/Closed) replaces the tiny cramped Accessible/Emergency-Safe/Closed segmented controls in both Waypoint and Navigation Path properties, preserving keyboard/focus behavior; entrance-linked node Position in the inspector is derived/read-only. `useToast` gained an explicit `info` case (previously fell through to the default). Verification passed: focused `navigationGraph.test.ts` (20) + `CampusEditor.navGraph.test.tsx` (70, 94 total) — attachment, drag safety, dedup, delete cleanup, visual, and UI regressions — full `pnpm test` (55 files / 696 tests), `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning, and `git diff --check` clean (CRLF normalization warnings only). Known limitations: `NavigationEdge.closed` is stored/editable but not yet filtered by `findNavigationRoute` (route filtering arrives with B8); indoor Floor Editor graph authoring, cross-floor stairs/elevator links, entrance indoor↔outdoor links, route testing, student routing, and SOS pathfinding remain later B5 phases — not started. No commit made.
- B5 Phase 1.7 — Outdoor Navigation Manual-QA Corrections pass (August 10, 2026): manual browser QA found real interaction defects, all fixed. **Campus toolbar simplification:** the generic Add POI (legacy Marker) and Draw Walkway tools are now HIDDEN from the normal Campus toolbar — their data model + persistence stay intact for legacy content and a future physical Pathway/Road feature, but no toolbar exposes a tool that cannot be selected/edited consistently; Navigation never exposed either tool. **Navigation Select isolation:** Navigation mode is now strictly graph-only after EVERY Add Waypoint completion, Connect Path completion/cancel, entrance-node creation/reuse, undo/redo, and property edit — clicking/dragging a Building never selects/moves/resizes it (`handleResizeStart`/`handleRotateStart` gained a nav-mode guard, `onEntranceDown` no longer falls through to B3 entrance editing/deletion in Navigation mode), and B3 entrances stay fully editable in Campus mode. **Entrance dashed-preview fix:** the old static green dashed entrance-connector block was removed from Canvas entirely (it was the ghost-line source), and completed entrance connections clear the transient preview + entrance hover state; the svg-click Path branch was reordered so `findEntranceAtPoint` is checked BEFORE the building-body rejection (entrances sit exactly on the inclusive building edge and were being rejected), and the svg-click path now routes through the canonical `commitNavEdgeWithEntrance` helper (fixing a stale-`navNodes` closure that could create a dangling edge). **Duplicate entrance-path safety:** re-attempting an existing A↔Entrance connection is rejected with the shared "Those points are already connected" toast — no duplicate entrance node, no duplicate edge, transient Connect state cleared. **Delete/Backspace shortcut:** selected single/multi nav nodes+edges delete via Delete/Backspace as ONE history action with connected-edge cleanup (new shared hoisted `deleteNavSelection` callback used by both the keyboard handler and the multi-props panel), with an input/contenteditable focus guard so text editing is never hijacked; ShortcutCheatSheet documents the shortcut. **Waypoint types:** UI now supports `safe_area` (new type; all UI types map to DB-safe `node_type` values via `normalizedNodeType` — `emergency_exit→exit`, `assembly→assembly_area`, `safe_area→assembly_area` — with the full type round-tripping through row metadata JSON, since migrations are out of scope); entrance-linked nodes LOCK their Type to Building Entrance so a generic Type change can never silently break the B3 entrance relationship. **Properties redesign:** Waypoint (General/Name, custom Type menu, Routing Accessible Yes/No + custom reason selector when No, Connections, compact Position, Delete) and Navigation Path (Connection From/To/Distance, segmented ⇄ Bidirectional / → One Way, Route Availability with segmented Accessible Yes/No + custom reason, Emergency Safe Yes/No + custom reason, Open/Closed) now use custom NaviSync controls only — no native selects anywhere (`combobox` query returns null); new `inaccessibleReason` field on nodes (mirroring edges) round-trips through metadata. **Node visuals:** special waypoint roles (entrance ring, destination, emergency-exit, assembly, safe-area shield) get modest distinct indicators without making the graph noisy. **Mode switch:** Campus ↔ Navigation ↔ Events are modes of ONE campus draft — switching never shows an unsaved dialog and never marks dirty; real graph changes keep Save enabled across switches. Verification passed: focused `CampusEditor.navGraph.test.tsx` (51 tests: mode isolation, entrance transient cleanup + duplicate reject, delete shortcut incl. input guard, custom controls, type lock, mode-switch dirty), full `pnpm test` (55 files / 675 tests), `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning, and `git diff --check` clean (CRLF normalization warnings only). Known limitations: `NavigationEdge.closed` is stored/editable but not yet filtered by `findNavigationRoute` (route filtering arrives with B8); entrance re-linking/cleanup when a B3 entrance is edited or removed is next-phase work; indoor Floor Editor graph authoring, cross-floor stairs/elevator links, entrance indoor↔outdoor links, route testing, student routing, and SOS pathfinding remain later B5 phases — not started. No commit made.

- B5 Phase 1.6 — Navigation Foundation UX Finalization pass (August 10, 2026): finalized the outdoor navigation authoring UX as the reusable foundation for future Floor Editor navigation. **Campus toolbar audit:** Campus keeps physical-map tools only (Select, Pan, Add POI, Add Building, Draw Walkway). The generic Marker is retained but relabeled **Add POI** with an explicit physical-point-of-interest tooltip — it is consumed by the public/student map as a real destination, so it is not dead code; Navigation never exposes it. Walkway is retained as **Draw Walkway** with an explicit tooltip that it draws visible physical walkway geometry and is NOT routing (nav edges remain the routing graph); no new walkway system was built. **Navigation toolbar:** focused Select / Pan / Add Waypoint / Connect Path / Remove with clear tooltips + aria-labels ("Place a routing point.", "Connect waypoints to build the walking network."). **Navigation Select foundation:** selects only graph elements — single click on waypoint or edge; Shift-click multi-select; drag-marquee over nodes (circle-rect intersect) and crossing edges (segment-rect intersect) via new pure helpers `navGraphSelectionIdsInRect` / `segmentsIntersectRect` in `src/lib/navigationGraph.ts`; campus geometry stays context-only (buildings can't be selected/moved/rubber-banded/erased). Multi-selected nodes drag as one rigid group (new `navGroupOriginRef` + shared post-state history entry — one undo restores the whole group), and edge geometry derives from endpoints so edges can never be dragged away from their nodes. **Add Waypoint vs Connect Path:** Add Waypoint = deliberate planning (click valid outdoor location → create → select → properties → return to Select). Connect Path = rapid network drawing; empty-space clicks auto-create canonical `NavigationNode`s, and hovering empty space now shows a small ghost preview node so the auto-create is never mysterious ("Click to create waypoint and connect" status). **Building-body rejection:** outdoor waypoints are rejected inside a building footprint with "Connect through a building entrance" feedback (new `pointInBuilding` in `src/lib/editorPlacement.ts` uses the rotated/expanded building body rect); Connect Path endpoints on a building body are rejected the same way. **Entrance preparation:** hovering a building entrance with Add Waypoint / Connect Path highlights the entrance as a special routing target (`findEntranceAtPoint` in `src/lib/buildingEntrances.ts`); clicking an entrance creates/reuses ONE canonical entrance-linked `NavigationNode` (`type: "entrance"`, new `entranceId` + `buildingId` fields, resolved world position) via shared `ensureEntranceNode`/`entranceNodeForEdge` helpers — no duplicate nodes per click; full indoor↔outdoor linking is still a later B5 phase. **Waypoint Properties redesign:** sectioned Waypoint inspector — General (Name, custom human-readable Type selector with Outdoor Waypoint / Building Entrance / Destination / Emergency Exit / Assembly Area / Safe Area — no native select, no raw IDs), Routing (Accessible + special role), Connections (count + isolated warning), compact Position, Delete Waypoint. **Edge Properties redesign:** NAVIGATION PATH inspector with CONNECTION (From/To names, Distance in world units — never meters), segmented Direction (⇄ Bidirectional / → One Way), ROUTE AVAILABILITY (Standard / Accessible / Emergency Safe / Closed with "Temporarily unavailable for routing." helper text). Navigation multi-selection gains a compact summary section (Waypoints/Connections count, Mark closed bulk flag, Delete Selected — one history action each). **Visual polish:** waypoint nodes got `group/nav` hover state, multi-selected rings, closed-edge dashed+dimmed appearance, and an entrance hover ring in the entrance overlay; graph remains readable over buildings. History/dirty preserved: real mutations mark the Campus draft dirty, selection/marquee/hover never do, group moves are one history gesture. Verification passed: focused `CampusEditor.navGraph.test.tsx` (35) + `navigationGraph.test.ts` (17) + `editorPlacement.test.ts` (point-in-building), full `pnpm test` (55 files / 659 tests), `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning, and `git diff --check` clean. Known limitations: `NavigationEdge.closed` is stored/editable but not yet filtered by `findNavigationRoute` (route filtering arrives with B8); indoor Floor Editor graph authoring, cross-floor stairs/elevator links, entrance indoor↔outdoor links, route testing, student routing, and SOS pathfinding remain later B5 phases — not started. No commit made.
- B5 Phase 1.5 — Navigation Authoring UX / Tool Architecture Correction pass (August 10, 2026): manual browser QA of Phase 1 exposed real UX/integration problems — near-identical toolbars across all five layers, the old Marker concept exposed in Navigation, a Marker inspector instead of a Waypoint inspector, waypoints appearing stuck at x=0,y=0 instead of the clicked location, and Accessibility/Emergency looking like separate drawing systems. Root cause of the 0,0 bug: the editor body was remounted on layer switch (`AnimatePresence mode="popLayout"` + `key={editor-${layer}}`), and when the exiting layer's editor unmounted, its `<svg ref={svgRef}>` cleanup set the shared `svgRef` to null in real browsers (jsdom never completes the exit animation, so tests could not observe it), making every subsequent `getPoint()` fall back to `{x:0,y:0}`. Fix: the editor body now stays mounted across layer switches (`switchLayer` already resets all transient tool state), so `svgRef` is never torn down — placement is correct at the default, zoomed, and panned cameras (regression tests read the camera from the svg content-group transform and assert the node's world→screen projection lands exactly on the click). Authoring structure consolidated to three layers — Campus, Navigation, Events — with Accessibility and Emergency becoming routing properties of ONE navigation graph (edge/node `accessible` / `emergencySafe` / `closed` flags; new `emergency_exit` and `assembly` node types; no frozen root doc requires separate accessibility/emergency tabs — `docs/06_IMPLEMENTATION_ORDER.md` B5 specifies them as graph properties). Toolbars are now contextual per layer via the previously-unused `LAYER_TOOLS` config (Navigation exposes Select / Pan / Add Waypoint / Connect Path / Remove with text tooltips + accessible names; generic Marker/Path naming is gone from Navigation; Campus keeps physical-map tools only, with Walkway for outdoor paths). The waypoint tool creates a real `NavigationNode` (never a legacy `CampusMarker`) with a compact graph-node visual (selected ring, destination-hover ring, dimmed when not accessible, red/amber rings for emergency_exit/assembly), opens the Waypoint inspector (heading Waypoint, never Marker), and returns to Select. Navigation Select only touches graph elements — campus geometry is context-only and cannot be selected/dragged/deleted/double-click-renamed, rubber-band is disabled, and tool shortcuts are layer-aware (e.g. B is inert in Navigation mode). Connect Path shows a live would-be-waypoint preview on empty-space hover before the first click, then the dashed start→pointer line with snapped node targets; Escape/right-click/tool-switch still cancel cleanly. Edge properties show distance in world units (not meters), Direction, Accessible, Emergency Safe, and a new Closed toggle; waypoint properties gained Emergency Exit / Assembly / Safe Area node types. Empty-state copy references Add Waypoint + Connect Path. Verification passed: focused `navigationGraph.test.ts` (17) + `CampusEditor.navGraph.test.tsx` (25), full `pnpm test` (55 files / 638 tests), `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning, and `git diff --check` clean. Known limitations: `NavigationEdge.closed` is stored/editable but not yet filtered by `findNavigationRoute` (route filtering arrives with B8); nav history uses the post-state pattern vs the legacy pre-state `upd()` (pre-existing quirk); indoor Floor Editor graph authoring, cross-floor stairs/elevator links, entrance indoor↔outdoor links, route testing, student routing, and SOS pathfinding remain later B5 phases — not started.
- B5 Phase 1 — Navigation Graph Foundation + Basic Outdoor Node/Edge Authoring pass (August 10, 2026): the existing navigation model was already rich (typed `NavigationNode`/`NavigationEdge` in `map-builder/types.ts`, `navigation_nodes`/`navigation_edges` Supabase tables with metadata round-trips in `campusStructureService.ts`, `navNodes`/`navEdges` cloning + public-adapter safety in `campusHelpers.ts`, `findNavigationRoute`/`buildTransitionEdges` in `pathfinding.ts`, and the Navigation layer tab) — but there was NO UI to create nodes or edges (the navigation layer's Path tool only dropped decorative `CampusPath` polylines). Phase 1 turns the existing Path/Waypoint tools into a reliable node-and-edge authoring foundation. New pure helpers in `src/lib/navigationGraph.ts` (createNavNode/createNavEdge with auto world-unit distance, findDuplicateNavEdge both directions, isSelfEdge, removeNavNode with connected-edge cleanup, findNavNodeAtPoint, graph validation basics) are shared by every entry point so the rules cannot diverge. In the Navigation layer the Marker tool becomes the Waypoint tool (creates a real `NavigationNode`, selects it, returns to Select), and the Path tool authors edges: click a node → click a second node → bidirectional edge (one history action, edge selected); clicking empty space auto-creates a waypoint there with the same canonical helper (second click creates the destination node AND connects in ONE history action); a live preview line with a node hit-target follows the pointer and never mutates the graph; Escape, tool switch, layer switch, or right-click cancels an incomplete edge without leaving an orphan (a `nav-path-status` chip shows "Select or place start point" / "Select or place destination"). Self-edges and duplicate edges (either direction) are rejected with clear toast feedback. Nodes are selectable, movable (grid-snapped, canvas-clamped, one-gesture history), and deletable via the Erase tool or Delete key with deterministic connected-edge removal; edge properties (direction/accessible/emergency-safe/closed/distance) and node properties (name/type/accessible/connections/isolated warning/delete) live in the existing PropertiesPanel sections. All nav mutations use the post-state history pattern (`onUpdate(next); pushHistory(next)`) so undo/redo restore exact node+edge states; real graph edits mark the Campus draft dirty (outer Save enables) while selection/hover/preview never do. Canvas renders nodes/edges only in the Navigation layer with selected/hover states, one-way arrowheads, and a constrained preview. Verification passed: focused `navigationGraph.test.ts` (17) + `CampusEditor.navGraph.test.tsx` (10), full `pnpm test` (55 files / 626 tests), `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning, and `git diff --check` clean. Known limitation: the nav history uses the post-state pattern while legacy marker/path `upd()` calls keep the pre-state pattern (a pre-existing quirk), so undo/redo is exact for nav-only sequences; a mixed marker→waypoint sequence may skip a step across the boundary (untouched, out of B5 scope). Indoor graph authoring, cross-floor stairs/elevator links, entrance indoor↔outdoor links, route testing, student routing, accessible routes, and SOS pathfinding are later B5 phases — not started.
- B4 COMPLETE checkpoint (August 10, 2026): the combined B4 manual final-gate retest has PASSED and B4 — Floor Editor Overhaul is marked COMPLETE. Final automated verification on `feature/floor-editor-overhaul` passed once more: `pnpm test` 53 files / 599 tests, `pnpm build` (existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning only), and `git diff --check` clean. Diff audit found no debug logs, TODO/FIXME markers, merge markers, package.json/pnpm-lock changes, or accidental UI exposure (`ADVANCED_FLOOR_REFERENCE_ENABLED` remains `false`, so Import/Calibration/Measure stay dormant). One checkpoint commit was created: `feat: complete floor editor overhaul`. Do NOT merge to main or push without an explicit owner decision; do not start B5 on this branch. wall CREATION now uses the exact same structural snap pipeline as wall ENDPOINT editing — there is no separate weaker wall-drawing snap. `findWallSnapTarget` (priority: existing wall endpoint → wall segment → room corner → room edge → floor boundary → grid/angle) is shared by the first point (`wallStartCursor`), the live preview and final commit (`wallDrawCursor`), and endpoint editing (`wallEndpointCursor`). New: while the Wall tool is armed but no start point is placed, hovering now shows live snap feedback for the point the first click will commit (start-point hover indicator), so wall creation visibly connects to existing walls just like endpoint editing; structural snaps always beat grid/angle assistance (resolved from the raw pointer first, and never moved away from afterwards), the preview line uses the exact snapped coordinates that are committed (no unsnapped preview that snaps only after click), snapping to a room-attached wall stays wall-to-wall (no room anchor is created on the new wall), and snapping to managed perimeter walls works without making them selectable. One wall = one history entry; undo removes the wall and redo restores the exact snapped geometry; completing a wall still returns to the Select tool with the new wall selected. New regression coverage in `FloorEditor.phase1_7.test.tsx` (9 new tests: start-hover indicator before first click, diagonal wall start+end snap, live preview = committed geometry, room-attached wall snap without room anchor, structural beats 20-unit grid, structural beats 45° angle, managed perimeter snap, post-create Select return, one-history undo/redo with exact geometry restore). Verification passed: focused `FloorEditor.phase1_7.test.tsx` (29 tests), full `pnpm test` (53 files / 599 tests), `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning, and `git diff --check` clean. B4 status is `COMPLETE`; the combined manual final-gate retest passed and the B4 checkpoint commit was created (see the top handoff note).
- B4 final validation / floor-editor dirty-state integration pass (August 10, 2026): the root cause of "Floor Editor changes do not enable the outer Save" was that the outdoor `CampusEditor` initialized its dirty baseline from the current campus on mount and fully unmounts while the Floor Editor is open — so returning to the outdoor view could never see floor edits as dirty. The fix keeps ONE source of truth: `AdminMapBuilderPage` now maintains a per-campus JSON baseline of the last PERSISTED campus (`savedSnapshotsRef`), set at hydration (`goToCampus`), after structure saves (`saveCampusStructure`), and after metadata writes (`updateCampusMetadata`), and passes it into `CampusEditor` via a new optional `savedSnapshot` prop; the editor compares its live draft against that persisted baseline, so any Floor Editor mutation (add/rename/duplicate/reorder/delete floor, room/wall/door/window/furniture/circulation/text edits, Floor Settings apply, grid changes, lock/hide/z-order) enables the outer Save, and opening/switching floors without editing stays clean. Floor Editor edits update the SAME canonical campus draft (no separate floor-local save model); the outer Save persists everything together, clears dirty, and re-disables. Undo back to the saved baseline returns the outer Save to disabled (JSON snapshot comparison), and the existing back/discard dialog counts floor changes as unsaved (discard restores the last saved campus). No-op interactions (selection, zoom, pan, switching floors without editing, cancelling Floor Settings) never enable Save. Persistence audit passed: serialize/hydrate round-trips every B4 field through `map_elements.metadata.ui` + `floors.metadata.ui` (rooms, walls, anchors, doors/windows incl. type/hinge/swing, furniture, circulation, labels, zOrder→z_index, visible→is_visible, managed perimeter metadata, floor order via `display_order`/`floor_number`, names, dims, grid) with UUID managed-perimeter IDs; the public/student adapter reads only rooms/elevators/stairs with optional chaining and safely ignores B4-only fields. New page-level integration tests (`AdminMapBuilderPage.floorDirty.test.tsx`, 12 tests) cover floor add/rename/duplicate/reorder/delete → outer Save, room + text edits → outer Save, Floor Settings apply vs cancel, no-op zoom/selection stays Saved, save clears dirty + persists, undo-to-baseline disables Save, and discard restores the saved campus. Their navigation helpers use a bounded click-until-mounted loop so a post-mount re-render can never silently swallow a row/breadcrumb click under parallel load. Verification passed: focused `AdminMapBuilderPage.floorDirty.test.tsx` (12), full `pnpm test` (53 files / 590 tests), `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning, and `git diff --check` clean. B4 status is `IMPLEMENTATION COMPLETE — MANUAL FINAL GATE PENDING`; the final manual checklist below remains required before marking B4 fully complete / merging.
- B4 floor management UX consistency pass (August 10, 2026): floor management is now obvious and consistent across the Floor Editor, the outdoor Hierarchy panel, and the Properties sidebar. The `...` button beside the floor tabs keeps its "Floor actions" tooltip/aria-label and opens a shared `FloorActionsMenu` (Rename Floor, Duplicate Floor, Move Left/Move Right, Floor Settings, Delete Floor) with Move Left disabled for the first floor, Move Right disabled for the last, and Delete disabled when only one floor remains. Right-clicking any floor tab opens the same menu targeted at THAT floor (duplicate switches to the copy; reorder keeps the active floor by ID; deleting an inactive floor leaves the active floor untouched). Rename Floor is now a direct action: a compact dialog trims whitespace, rejects empty names with inline guidance, updates tabs/breadcrumb/hierarchy immediately, and persists through the existing save path. Delete from every surface uses the same confirmation and now reports the authored item count ("Delete Floor 2 and its 14 authored items?"). The Hierarchy panel floor rows expose the same shared `...` menu (Rename, Duplicate, Move Up/Move Down, Delete), support drag-and-drop reorder with the existing HTML5 pattern (grip handle, insertion indicator, click-suppression so a reorder never opens a floor), and their Add Floor button remains. The Floor Overview sidebar (shown only when no object is selected) is now a compact floor section with General (Name), Canvas (Width/Height via the same shrink-validated single-edit apply path as Floor Settings), Grid (Show Grid toggle + Grid Size 10/20/40 presets), and Actions (Open Floor Settings, Duplicate Floor, Move Up/Move Down, Delete Floor). All surfaces route through one shared `src/lib/floorManagement.ts` (add/rename/duplicate/move/delete + authored-item count) and one shared `FloorActionsMenu` component, so no surface has a separate implementation; horizontal tabs use Move Left/Right and the hierarchy uses Move Up/Down on the SAME floor array. Active floor is tracked by ID: reorders never switch it, and deleting the active floor activates the nearest remaining floor (next at the same index, else previous). Object selection still wins in the Properties sidebar. Verification passed: focused `FloorEditor.phase1_8.test.tsx` + `CampusEditor.floorHierarchy.test.tsx` + `floorPlanNormalization.test.ts` (122 tests), full `pnpm test` (52 files / 578 tests), `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning, and `git diff --check` clean. The 12-step manual browser retest below is still required; B4 remains incomplete.
- B4 live text editing + floor management workflow pass (August 10, 2026): inline label editing grows/shrinks the on-canvas editor from the live draft value (real canvas measurement in production browsers, deterministic per-character fallback under JSDOM — no canvas test dependency), hides the committed SVG text underneath while editing, Escape restores the original value, and one completed edit commits exactly ONE history action regardless of keystrokes. Floor management now supports Add Floor (new default 600x450 floor that becomes active), Duplicate Floor (deep copy with fresh IDs and remapped Room→Wall anchors plus Door/Window `wallId` references), Move Left/Right reordering (persisted through the existing floor order), and Delete Floor with a confirmation dialog plus last-floor protection. Floor tabs expose Add Floor and a compact floor actions menu; active floor/tab state stays correct after add/duplicate/reorder/delete. Grid Size presets (10/20/40) persist through Floor Settings, drive both the visual grid and the snapping spacing, and never move existing objects; the toolbar Grid Snap toggle remains the snap on/off control. Legacy 10-unit snap regressions now pin `gridSize: 10` explicitly in their fixtures instead of relying on the old effective grid. Verification passed: focused `FloorEditor.phase1_8.test.tsx` + `floorPlanNormalization.test.ts` (104 tests), full `pnpm test` (51 files / 563 tests), `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning, and `git diff --check` with CRLF normalization warnings only. The 12-step manual browser retest below is still required; B4 remains incomplete.
- Latest B4 text/modal manual-QA fixes pass (August 10, 2026): ColorPicker popovers now clamp to a comfortable viewport margin and flip above low triggers without transform drift, keeping bottom-of-modal color selection usable. Text labels now expose a transparent body hit target so direct clicks select and direct drags move the label, while double-click opens isolated inline edit with a caret; opening edit is guarded against same-tick blur so it no longer instantly closes, and typing arms normal blur commit. Multi-selected text does not enter edit from ordinary typing; double-clicking a text label from a multi-selection collapses to that label and enters text-only edit. Single selected text labels now show on-canvas resize handles that change font size cleanly plus a rotate handle, with handles hidden during inline edit. Verification passed: focused `ColorPicker.test.tsx` + `FloorEditor.phase1_8.test.tsx` + `FloorEditor.uxParity.test.tsx` (101 tests), full `pnpm test` (51 files / 554 tests), `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning, and `git diff --check` with CRLF normalization warnings only.
- Latest B4 floor view/default cleanup + import/calibration deferral pass (August 10, 2026): new/default floor canvas dimensions now come from the shared `DEFAULT_FLOOR_CANVAS` constant at 600x450, while explicitly persisted floor sizes (including larger 1200x800 records) remain unchanged and authored content is never auto-resized. The previously observed large default behavior came from floor persistence/hydration saving floor rows with campus-level canvas dimensions instead of floor-specific dimensions; the serializer now writes normalized floor-specific canvas width/height and preserves custom floor sizes. Entering or switching to a floor now auto-fits the floor into the available editor viewport with padding by changing zoom/pan only; Floor Width/Height remain authored world dimensions. Floor-plan import, calibration, and Measure are deferred from the normal admin workflow: Floor Settings again shows only Floor Name, Width, Height, Background Color, Grid, and Perimeter Wall; background/calibration metadata still normalizes safely but the reference layer, calibration points, calibration panel, and Measure tool are hidden while the advanced reference feature flag is off. Verification passed: focused cleanup tests and full `pnpm test` (50 files / 544 tests).
- Deferred B4 floor-plan import + calibration implementation note (August 10, 2026): dormant helper/type/service code exists for a future optional reference/tracing feature using the documented private Supabase Storage `floor-plans` bucket, signed preview URLs, background metadata, and canonical `metersPerUnit` calibration. It is intentionally not visible in the normal Floor Editor workflow after the cleanup pass.
- Latest B4 Phase 3.1 Double Door visual correction pass (August 10, 2026): Double Door rendering was narrowed to a visual-only correction. Single Door, UUID/perimeter persistence, data model, windows, anchors, floor resize, shortcuts, and selection behavior were not changed. The Double Door glyph now draws two mirrored single-door-style leaves hinged at the left and right jambs, with each half-door opening perpendicular from its jamb on the selected Swing Side and each swing arc running from the center meeting point to its own open leaf. This removes the pinched near-center diagonal geometry that could still read as an X/diamond, while preserving one shared wall cutout, total-width resize handles/selection outline, parent-wall transforms for horizontal/vertical/diagonal/perimeter walls, and unchanged persistence. Phase 3.1 verification passed: focused `FloorEditor.phase1_8.test.tsx` (81 tests), full `pnpm test` (48 files / 535 tests), `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning, and `git diff --check` with CRLF normalization warnings only. Browser visual QA still requires an authenticated/local manual admin-editor session.
- B4 Phase 1 + 1.5 + 1.6 + 1.7 + 1.8 + 1.9 + 2.0 + 2.1 + 2.2 + 2.3 + 2.4 + 2.5 + 2.6 + 2.7 + 2.8 + 2.9 + 3.0 + 3.1 are implemented on `feature/floor-editor-overhaul` after the Phase 0 audit/planning pass. Current status: ready for combined B4 foundation manual retest after Phase 3.1 verification; do not mark B4 complete, commit, merge, or start later B4 structural/navigation phases from this pass.
- Latest B4 Phase 3.0 managed perimeter UUID persistence + Double Door visual correction pass (August 9, 2026): managed perimeter walls now use UUID-compatible wall IDs instead of synthetic `managed-perimeter-{floorId}-{side}` IDs; side/managed identity is preserved through `managedKind: "perimeter"` and `perimeterSide`. Floor normalization migrates legacy synthetic perimeter wall IDs to UUIDs and remaps attached Door/Window `wallId` references before editor state and the Supabase structure serializer build rows, so no synthetic perimeter ID reaches `map_elements.id` or opening metadata in the save payload. Existing UUID perimeter IDs are reused by side during floor resize and perimeter style/thickness/material changes. Double Door rendering was corrected to behave like two single leaves sharing one opening: left and right leaves hinge from their jambs toward separate near-center endpoints, arcs match each leaf, Swing Side flips both leaves to the opposite wall side, and the symbol remains transform-driven for horizontal/vertical/diagonal/perimeter walls without the prior X/diamond shape. Phase 3.0 verification passed: focused `FloorEditor.phase1_8.test.tsx`, `floorPlanNormalization.test.ts`, and `campusStructureService.test.ts` (96 tests), full `pnpm test` (48 files / 535 tests), `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning, and `git diff --check` with CRLF normalization warnings only. Browser visual QA still requires an authenticated/local manual admin-editor session.
- Latest B4 Phase 2.9 door width consistency + door types + perimeter entrance validation pass (August 9, 2026): Door rendering now uses one effective opening width for wall cutout, jambs, hinges, leaves, swing arcs, hit target, selection outline, resize handles, clamping, and persisted width; the previous capped leaf/arc length no longer creates a large empty wall gap for wide Doors. Door resize handles now resize from the dragged wall-edge while anchoring the opposite edge, so either handle can grow or shrink continuously within type-specific min/max and parent-wall safety clamps, including unlocked Doors on managed perimeter walls. Doors now persist `doorType` (`single`/`double`) through existing floor metadata with no migration; Single Doors keep one hinge/leaf/arc plus Hinge Left/Right and Swing Side A/B, while Double Doors render one shared opening with paired jamb hinges, two leaves meeting near center, and two coordinated arcs, hide the single-hinge control, and keep Swing Side A/B. Single->Double validates the parent wall can fit the double minimum and safely raises/clamps width, while Double->Single preserves/clamps total width. Attached Door validation is now parent-wall-opening based, so managed perimeter entrance Doors remain valid even if decorative swing/leaf geometry extends outside the floor canvas; unattached out-of-bounds Doors remain invalid. Phase 2.9 verification passed: focused `FloorEditor.phase1_8.test.tsx` (80 tests), focused `floorGeometry.test.ts` (20 tests), full `pnpm test` (48 files / 532 tests), `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning, and `git diff --check` with CRLF normalization warnings only. Browser visual QA still requires an authenticated/local manual admin-editor session.
- Latest B4 Phase 2.8 opening corner safety + orientation/resize + perimeter editing + property UI polish pass (August 9, 2026): wall-attached Door and Window placement, drag, property edits, persistence sync, and canvas resize now share the same wall-opening clamp so openings stay fully inside the parent wall segment with a safety inset on horizontal, vertical, diagonal, and managed perimeter walls. Doors now carry independent `hinge` and `swingSide` metadata, default perimeter doors inward, expose property segmented controls plus context-menu Flip Hinge / Flip Swing Side actions, and preserve legacy `direction` compatibility for left/right hinge state. Windows remain orientation-free because the rendered symbol is symmetric. Selected wall-attached Doors and Windows expose exactly two along-wall resize handles, slide along managed perimeter walls even though the perimeter wall itself is non-selectable/fixed, and remain blocked by true user-locked parent walls. Door/Window properties now use compact polished controls instead of the old native swing-direction select. Phase 2.8 verification passed: focused `FloorEditor.phase1_8.test.tsx` (73 tests), focused `floorGeometry.test.ts` (18 tests), full `pnpm test` (48 files / 523 tests), `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning, and `git diff --check` with CRLF normalization warnings only. Browser visual QA still requires an authenticated/local manual admin-editor session.
- Latest B4 Phase 2.7 opening clipping + perimeter interaction + deselect fix pass (August 9, 2026): managed perimeter walls now remain structural/snap/opening targets but are not selectable editor objects; direct clicks and context menus on them no longer open wall properties, Ctrl+A excludes them, and normal wall selection remains intact. Placed wall-attached Door and Window symbols now render outside the floor content clip so wall cutouts, jambs, door hinges/leaves/swing arcs, window glazing, hit targets, and selected outlines are not clipped on interior or perimeter walls; managed perimeter openings flip their architectural detail inward toward the visible floor side while preserving the opening centerline on the wall. Blank select-mode clicks now clear both single and multi-selection on empty floor/SVG/workspace areas, including plain clicks inside a multi-selection bounding box, while actual group dragging and UI/control clicks remain preserved. Phase 2.7 verification passed: focused `FloorEditor.phase1_8.test.tsx` (67 tests), focused `floorGeometry.test.ts` (18 tests), full `pnpm test` (48 files / 517 tests), `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning, and `git diff --check` with CRLF normalization warnings only. Browser visual QA still requires an authenticated/local manual admin-editor session.
- Latest B4 Phase 2.6 door/window visual correction + selection boundary fix pass (August 9, 2026): attached Door and Window rendering now uses shared centered architectural symbols for preview and placed openings. Doors visibly cut the wall centerline and show jambs, hinge, leaf, and swing arc at normal zoom on horizontal, vertical, and diagonal walls; Windows visibly cut the wall centerline and show jambs, glazing rails, mullion, and frame detail on horizontal, vertical, and diagonal walls. The opening transform remains derived from wall endpoints, wall angle, normalized offset, width, and wall thickness, so the symbol center stays on the wall centerline and follows parent-wall propagation. Ctrl+A now selects only current-floor objects whose geometry intersects the canonical `0,0,width,height` floor canvas; completely out-of-bounds objects are excluded from Ctrl+A but remain directly selectable when targeted. Empty select-mode clicks inside the floor and on the SVG background outside the floor continue to clear selection, while object clicks, Shift-click multi-select, right-click menus, Ctrl+D, undo/redo, room/wall anchors, perimeter walls, and input-field Ctrl+A behavior are preserved. Phase 2.6 verification passed: focused `FloorEditor.phase1_8.test.tsx` (61 tests), full `pnpm test` (48 files / 511 tests), `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning, and `git diff --check` with CRLF normalization warnings only. Browser visual QA still requires an authenticated/local manual admin-editor session.
- Latest B4 Phase 2.5 manual QA corrections pass (August 9, 2026): placement-mode object groups now let events continue to the floor SVG instead of swallowing them, fixing clicks directly on visible walls for Door/Window placement and clicks inside Room geometry for Furniture/stairs/ramps/elevators/labels placement while preserving select/erase behavior. Door/window wall targeting now uses zoom-aware wall-centerline tolerance so horizontal, vertical, and diagonal wall bodies remain easy to hit; attached openings keep the Phase 2.3 normalized wall attachment behavior, slide bounds, and parent-wall propagation. Managed perimeter walls no longer expose normal endpoint edit handles when selected, and perimeter-only wall joints no longer render large permanent corner caps, while authored wall joints still render normally. Ctrl+D now duplicates the current single selection or current multi-selection, selects the copies, records one undoable history action, preserves Ctrl+D inside editable fields, and is documented in Floor Editor shortcuts. Phase 2.5 verification passed: focused `FloorEditor.phase1_8.test.tsx` (55 tests), focused geometry/normalization tests (23 tests), full `pnpm test` (48 files / 505 tests), `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning, and `git diff --check` with CRLF normalization warnings only. Browser visual QA still needs an authenticated/local manual pass in the running admin editor.
- Latest B4 Phase 2.4 door/window usability + editor shortcuts + room label readability + optional perimeter wall pass (August 9, 2026): door/window wall targeting now uses forgiving wall hit tolerance, larger default widths, a full candidate-wall highlight, and wall-aligned previews that show door leaf/swing arcs or window glazing; invalid empty-canvas placement still rejects with guidance and no free-floating opening is created. Attached doors/windows now render stronger wall cutouts, end caps, visible architectural glyphs, and larger transparent hit targets; sliding keeps normalized `offset`, clamps safely to parent wall length, and preserves Phase 2.3 room/wall propagation behavior. Ctrl+A now selects all selectable current-floor rooms, walls, doors, windows, furniture, stairs, ramps, elevators, and labels while preserving native behavior in inputs, textareas, selects, and contenteditable fields; locked selections remain selected but block transforms per admin semantics. Room fills are more translucent so the grid remains faintly visible, while room names moved to a semantic overlay above furniture/circulation and below explicit labels with a subtle backdrop/text halo. Floor Settings now has an explicit opt-in Managed Perimeter Wall section; enabling creates four locked structural walls with managed metadata and editable thickness/material/color, resizing the floor updates only those generated walls while authored edge walls remain unchanged, openings on perimeter walls stay attached through resize via normalized offsets, and disabling removes managed walls or confirms removal when attached openings exist. Phase 2.4 verification passed: focused `FloorEditor.phase1_8.test.tsx` (49 tests) and `floorGeometry.test.ts` (18 tests), full `pnpm test` (48 files / 499 tests), `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning, and `git diff --check` with CRLF normalization warnings only.
- Latest B4 Phase 2.3 robust room/wall attachment + wall-attached doors/windows + floor/canvas resize semantics pass (August 9, 2026): floor canvas expansion now changes only width/height while preserving authored world coordinates, anchors, and relationships; shrink validation checks synced authored geometry and blocks invalid sizes with category counts while allowing exact edge contact. Completed room-attached wall creation and endpoint editing now re-evaluate both endpoints when a wall is already room-attached, so full-edge and partial room-edge walls persist both normalized offsets and follow room move/resize/rotation without resizing the room. Doors and windows now place only on nearby walls, show a wall-aligned preview, persist `wallId` + normalized `offset` + width through existing metadata, select after placement, render as architectural wall openings, slide along the parent wall by updating offset, clamp width to wall length, and follow parent wall changes including room-driven wall propagation. Deleting a wall deletes attached openings in the same history action; duplicating a wall duplicates attached openings with remapped wall IDs and preserved offsets; full floor duplication also remaps opening `wallId`s. Locked parent walls block direct structural edits to attached openings while allowing parent-driven carry behavior; direct locked-object edits remain blocked. Phase 2.3 verification passed: focused `floorGeometry.test.ts` (18 tests) and `FloorEditor.phase1_8.test.tsx` (46 tests), full `pnpm test` (48 files / 496 tests), `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning, and `git diff --check` with CRLF normalization warnings only.
- Latest B4 Phase 2.2 room/wall attachment + floor-boundary visual fixes + wall authoring UX pass (August 9, 2026): wall endpoints can now persist lightweight room-edge anchors (`roomId`, edge, normalized offset) through existing floor metadata without a DB migration. Room move/resize/rotation drives attached wall endpoints, while wall endpoint edits never resize or move rooms; dragging a wall endpoint away clears that endpoint anchor and snapping to another room records the new room anchor. Room deletion keeps walls and clears anchors to the deleted room; duplicated rooms alone do not steal walls; group-duplicated room+wall sets remap copied anchors when the source room is also copied. Multi-selection room+attached-wall transforms skip room-driven propagation for selected walls to avoid double-transforming the wall. Locked attached walls block room transforms with a toast instead of being silently modified or detached. Room visuals are clipped to the floor canvas so centered SVG strokes cannot leak outside floor boundaries, while geometric floor constraints remain in force. Completed wall placement selects the new wall, opens its properties, and returns to Select; Shift-click completion keeps the wall tool active for continuous authoring, and the floor shortcuts/help now documents it. Wall length labels now use the wall direction plus perpendicular normal offset and rotate upright for horizontal, vertical, and diagonal walls. Phase 2.2 verification passed: focused `floorGeometry.test.ts` (14 tests) and `FloorEditor.phase1_8.test.tsx` (44 tests), full `pnpm test` (48 files / 490 tests), `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning, and `git diff --check` with CRLF normalization warnings only.
- Latest B4 Phase 2.1 floor object layering / visibility / lock + room / wall / circulation polish pass (August 9, 2026): room fills now render below walls and rooms have rotation-aware selection outlines, side/corner resize handles, rotate handles, editable size/rotation controls, floor-bounds clamping, and multi-selection move/resize/rotate participation. Wall snapping keeps the existing endpoint → wall segment priority, then adds room corners and room edges before floor boundary/grid/angle assistance. Floor rooms, walls, doors, windows, furniture, stairs, ramps, elevators, and labels now carry optional `visible`, `locked`, and `zOrder` UI metadata; properties panels, context menus, and the multi-selection panel expose Hide/Show, Lock/Unlock, and deterministic band-local layer ordering actions. Locked objects stay selectable/manageable but block move/resize/rotate/endpoint edit/delete/duplicate. Hidden objects ghost in the admin editor while remaining selectable/manageable; current structure serialization maps `zOrder` to `z_index` and preserves existing `is_visible` behavior, with no DB migration. Phase 2.1 verification passed: focused 58-test floor pass, full `pnpm test` with 48 files / 485 tests, and `pnpm build` with the existing non-blocking >500 kB `AdminMapBuilderPage` Vite chunk warning.
- Latest B4 Phase 2.0 transform / multi-selection / property UX completion pass (August 9, 2026): rotated furniture, stairs, ramps, and elevators now render selected outlines, side/corner resize handles, and rotate handles in the same rotated local frame as the object artwork; single-object resize projects drag deltas into the object's local axes and still respects per-type minimums, maximums, and rotated floor bounds. Rotation drags use the gesture start angle, support free rotation by default, and snap to 15-degree increments while Shift is held. Empty clicks inside a multi-selection bounding box now start a group move instead of clearing selection. Multi-selection frames now include side/corner resize handles and a rotation handle; group resize scales member layout from the group bounds, group rotation rotates selected furniture/circulation rotations plus wall endpoints around the group center, and both commit as one history step. Wall rendering makes the existing Material property meaningful through subtle concrete/brick/wood/glass/drywall line treatments, and the wall inspector replaces native dropdowns with compact NaviSync button groups. Save feedback is again a small top-center floating surface below the toolbar rather than a centered modal-like overlay. Keyboard history coverage now includes Ctrl+Z, Ctrl+Y, Ctrl+Shift+Z, and input-focus protection. Phase 2.0 verification passed: focused 54-test floor pass, full `pnpm test` with 48 files / 481 tests, `pnpm build`, and `git diff --check` with CRLF warnings only.
- Latest B4 Phase 1.9 visual/interaction polish pass (August 9, 2026): wall rendering now adds endpoint joint caps for exact shared endpoints so snapped wall joins visually meld without starting wall topology or room inference; snapped endpoint editing remains free and detachable. Wall/furniture/stairs/ramp/elevator/label inspectors use compact single-panel layouts with consistent General/Geometry/Actions-style control placement instead of tiny Info/Style/Size tab splits. Stairs, ramps, and elevators render as distinct structured top-down floor-plan symbols (stair treads + direction cue, ramp incline/handrail cue, elevator shaft/car/door cue), support persisted rotation, expose on-canvas resize and rotate handles, and keep bounds-safe resizing. Small selected-object handles now scale against object size and zoom so tiny objects remain readable. Save feedback is more noticeable through a centered real-async Saving floor... -> Saved overlay. Furniture remains curated at 9 default items with legacy types still rendering safely; no new redundant palette items were added. Phase 1.9 verification passed: focused floor tests passed (47 tests across `FloorEditor.phase1_8.test.tsx`, `floorGeometry.test.ts`, and `floorPlanNormalization.test.ts`); full `pnpm test` passed with 48 files / 474 tests; `pnpm build` passed with the existing non-blocking >500 kB `AdminMapBuilderPage` chunk warning; `git diff --check` passed with CRLF normalization warnings only.
- Latest B4 Phase 1.8 interaction-refinement pass (August 9, 2026): multi-selection now persists through group drags and right-clicks (Figma/outdoor-editor style), a subtle group bounding outline follows the selection live, right-click on empty canvas never selects objects behind it, and a dedicated Selected Objects inspector (Duplicate/Delete/Clear, no canvas or single-object settings) replaces the old mixed panel; nothing selected shows a neutral Floor Overview with an explicit Open Floor Settings button. Walls snap from the RAW pointer so endpoint → segment → boundary always beats grid/angle and the committed endpoint is EXACT (segment snapping no longer rounds, so diagonal connections meet with no 2–5px gaps); endpoint editing is free by default (Shift = 15° angle assist) so snapped walls detach and re-angle naturally. The default furniture library was trimmed to 9 genuinely distinct items (Chair, Bench, Sofa; Desk, Table; Cabinet, Shelf; Computer Workstation; Plant — Student Chair / Student Desk / Teacher Desk removed from the palette but legacy data still renders safely), every `FloorFurnitureSymbol` branch is now structurally distinct and honors the functional Color property, the furniture inspector is one unified panel (Identity / Appearance & Size / Actions), and resize adds side handles (single-dimension), Shift aspect-ratio on corners, and rotated-bounds containment. Text annotations are real editable objects (select or double-click → autofocused Text control, font size, color, alignment, rotation; edits stay inside the floor). Floor Settings became a polished dialog (General name / Canvas width-height / Appearance background presets + custom color + grid toggle) kept fully separate from object properties, and the floor surface + grid visibility persist through save/load and undo. Save now shows a central Saving floor… → Saved overlay tied to the real async operation (toast remains secondary). Full `pnpm test` passed with 48 test files / 469 tests; `pnpm build` PASSED with only the existing non-blocking >500 kB `AdminMapBuilderPage` Vite chunk warning; `git diff --check` PASSED (CRLF normalization warnings only).
- Latest B4 Phase 1.7 continuation pass (August 8, 2026): an interrupted coding-agent pass left Phase 1.7 changes in the worktree; this pass verified them, repaired wall-snap priority (endpoint → segment → boundary so snapping systems no longer fight), made tool switches clear the transient snap indicator (and kept it alive for the full wall-drawing gesture), allowed keyboard Delete on marquee-only multi-selections, hardened Escape cleanup, and added the Phase 1.7 test suite. Full `pnpm test` passed with 47 test files / 442 tests; `pnpm build` PASSED with only the existing non-blocking >500 kB `AdminMapBuilderPage` Vite chunk warning; `git diff --check` PASSED.
- B4 Phase 1.7 multi-selection: marquee and modifier-click now cover rooms, furniture, stairs, ramps, elevators, labels, doors, windows, and walls. Floor paths remain intentionally excluded from multi-selection because their geometry is polyline-point based and group movement is unsafe; paths still support single selection and right-click duplicate/delete.
- B4 Phase 1.7 group behavior: dragging any selected member moves the whole mixed group as one rigid set inside the floor bounds; group delete removes every selected object in one floor update/one history step and clears selection; group duplicate gives every copy a new ID, preserves relative offsets with an in-bounds delta, and selects the duplicated set afterward; one undo reverses each group action.
- B4 Phase 1.7 wall snapping: new wall start, wall completion, and wall endpoint editing snap to existing wall endpoints first, then valid wall segments, then the canonical floor boundary, then grid/angle snap. A subtle transient snap indicator renders only while genuinely snapping at the exact target and clears on wall completion, tool switch, Escape, and floor switch.
- B4 Phase 1.7 furniture baseline: the same `FloorFurnitureSymbol` vector renderer draws palette previews and placed objects as recognizable top-down symbols (no text boxes); furniture resize has per-type minimums (8) and ceilings (chair/plant 32, bench 86/28, sofa 86/44, storage 54/42, desk/table 74/48); legacy/unknown types keep a safe structured fallback. Phase 1.8 curation trims the default library to 9 distinct items and makes every symbol structurally recognizable.
- B4 Phase 1.8 furniture: the default palette is intentionally small — Seating (Chair, Bench, Sofa), Tables/Work (Desk, Table), Storage (Cabinet, Shelf), Electronics (Computer Workstation), Decor (Plant). Redundant variants (Student Chair, Student Desk, Teacher Desk) were removed from the DEFAULT library only; previously saved legacy furniture types still render safely through their family branch or the structured fallback. `FloorFurnitureSymbol` was rewritten so common items are immediately recognizable (chair = seat + backrest; sofa = body + arms + cushion; bench = long seat + slats; desk = flat work surface + work zone/keyboard band; table = rounded tabletop + leaf; cabinet = doors + handle dots; shelf = visible shelf divisions; computer workstation = desk + monitor + keyboard; plant = planter + organic leaves) and the Color property is functional everywhere it renders (Option A — the Color control is real and persists). The furniture inspector is ONE unified panel (Name/Type, Width/Height, Rotation, Color, Duplicate, Delete) with the Info/Style/Size tab bar hidden for furniture.
- B4 Phase 1.8 furniture resize: selected furniture now exposes side handles (n/s/e/w resize one dimension, e.g. width on e/w) AND corner handles (width + height together; Shift preserves aspect ratio on corners). Minimums (8), per-type ceilings, canvas bounds, and rotated-bounds containment all apply — a rotated item's transformed world AABB is pulled back inside the floor so it never visibly extends outside during resize.
- B4 Phase 1.8 text annotations: Text Label is a real editable object. Placing text selects it and opens the inspector with an autofocused Text field; double-clicking a label opens the editor; the panel provides Text, Font Size, Text Color, Alignment (left/center/right — rendered via the correct text anchor), and Rotation. Text/font-size edits are constrained by rendered bounds so annotations never slide outside the floor.
- B4 Phase 1.8 Floor Settings dialog: a polished modal (matching Outdoor Map Builder canvas/campus-settings language) with General (Floor Name), Canvas (Width/Height draft), and Appearance (background color presets White/Warm White/Light Gray/Blueprint + custom ColorPicker, Show canvas grid toggle). Save Changes validates shrink through authored objects, applies everything as ONE history step, marks the floor dirty, and updates the rendered floor surface + grid visibility; Cancel/X discard the draft. Floor Settings is reachable from the toolbar, the canvas right-click menu, and the Floor Overview panel — and is never shown inside multi-selection or single-object properties.
- B4 Phase 1.8 Save feedback: the editor shows a central Saving floor… → Saved overlay (spinner → check) that responds to the real async save, with duplicate submission locked and the toast demoted to secondary feedback. On failure the overlay leaves cleanly, the error toast is visible, and edits + dirty state survive.
- B4 Phase 1.7 Save/Publish: the Floor Save button reflects the dirty state (disabled when clean), shows a real loading spinner and disables duplicate submission, clears dirty on success, and preserves edits + dirty on failure. Publish is visible but disabled with an A6 explanation when campus publishing is unavailable; it never creates an independent floor publication state and delegates only through the existing campus publish callback after floor-issue validation.
- B4 Phase 1.7/1.8 canvas settings lineage: Phase 1.7 introduced draft Width/Height with Save Changes/Cancel and shrink blocking; Phase 1.8 moved this into the Floor Settings dialog and added floor appearance (background color + grid visibility), keeping canvas/floor configuration fully separate from object and multi-selection properties.
- B4 Phase 1 normalization architecture: new `src/lib/floorPlanNormalization.ts` centralizes `normalizeFloor`, `createDefaultFloor`, `duplicateFloorForBuilding`, `normalizeFloors`, default floor labels, and floor undo-entry extraction. Normalization preserves IDs and authored objects, initializes `rooms`, `paths`, `walls`, `doors`, `windows`, `furniture`, `stairs`, `ramps`, `elevators`, and `labels`, repairs missing room `buildingId`/`floorId`, and avoids mutating source floor objects.
- B4 Phase 1 creation/default changes: BuildingWizardModal, FloorWizardModal, HierarchyPanel, and PropertiesPanel now create floors through the canonical helper; building and floor duplication remap floor IDs, room IDs/ownership, and nested floor-object IDs while preserving existing authored geometry/content.
- B4 Phase 1 floor switching behavior: FloorEditor normalizes its active floor, tracks dirty state against the last saved/loaded normalized floor baseline, and guards floor-tab switches with an unsaved-changes modal offering Save Changes, Discard Changes, and Keep Editing. Successful save awaits the existing save/update flow before switching and clears dirty state; failed save keeps the user on the current floor with edits and dirty state preserved; discard restores only the current floor to its baseline before switching; cancel keeps the active floor and edits.
- B4 Phase 1 history/selection safety: floor history is reseeded on floor ID changes so undo on Floor B does not mutate Floor A; stale selection, multi-selection, context menus, properties panel state, drag/resize refs, and incomplete drawing gestures are cleared on floor switch. The selected tool is preserved; incomplete geometry is cancelled.
- B4 Phase 1 persistence/save-reopen boundary: current structure serialization/hydration now normalizes floors so older/incomplete floor records do not crash the editor and save/load remains compatible with existing map element metadata. No DB schema or migration was added.
- B4 Phase 1 tests added/updated: `src/lib/__tests__/floorPlanNormalization.test.ts` covers legacy/incomplete floor defaults, canonical creation, no-crash undo entries, and duplicate remapping; `src/components/map-builder/__tests__/FloorEditor.floorSwitching.test.tsx` covers dirty prompts, save/discard/cancel switching, failed saves, selection/zoom/pan non-dirty behavior, incomplete drawing cancellation, and cross-floor history isolation; `src/components/map-builder/__tests__/FloorEditor.render.test.tsx` now covers missing-floor load safety.
- B4 Phase 1.5 UX/interaction parity: FloorEditor now exposes outdoor-editor-style Select/Pan controls, Ctrl+wheel zoom, Fit Floor, a non-collapsed More tools menu, marquee selection, constrained multi-object movement, boundary-constrained room resizing and object placement/movement, editable floor canvas dimensions with blocking shrink validation, an Issues modal, a floor-specific shortcut modal, and shape-based furniture visuals that avoid visible text labels on the canvas.
- B4 Phase 1.5 floor geometry helpers: new `src/lib/floorGeometry.ts` centralizes floor canvas defaults/minimums, item bounds, selection rectangle intersection, constrained translation, room resize clamping, and blocking geometry validation for authored objects outside the floor canvas. `src/lib/floorPlanNormalization.ts` now carries `canvasW`/`canvasH` through normalized floors and floor undo snapshots.
- B4 Phase 1.5 tests added/updated: `src/lib/__tests__/floorGeometry.test.ts` covers canvas sizing, group constraints, wall translation, room resizing, marquee selection, and outside-canvas validation; `src/components/map-builder/__tests__/FloorEditor.uxParity.test.tsx` covers toolbar/menu clarity, marquee selection without accidental panning, constrained grouped drag, canvas expansion/shrink blocking, Ctrl+wheel zoom, floor shortcuts, and furniture visual labels; `src/components/map-builder/__tests__/FloorEditor.render.test.tsx` now expects the unified Object Library.
- B4 Phase 1.6 UX completion/parity: FloorEditor now renders one canonical floor surface at `0,0,width,height`; the empty-state prompt is world-space centered inside the floor and disappears/returns with authored content; the decorative compass was removed; selecting or placing an object auto-opens a docked right-side inspector that reflows the canvas instead of overlaying it; furniture palette previews are visual and placed furniture has top-down symbols; furniture resize handles are bounded and commit as one history gesture; wall rendering is more architectural with boundary and endpoint snapping; right-click menus expose valid floor object actions only; the dead Measure affordance is hidden; Issues is a compact popover.
- B4 Phase 1.6 tests added/updated: `FloorEditor.uxParity.test.tsx` now covers canonical floor bounds, world-space empty state, compass removal, Measure hiding, docked auto-opening properties, right-click duplicate/delete, and bounded furniture resize handles. `FloorEditor.selection.test.tsx` now targets the canonical floor boundary instead of the retired inset fill.
- B4 Phase 1.8 tests added/updated: new `src/components/map-builder/__tests__/FloorEditor.phase1_8.test.tsx` (27 tests) covers persistent multi-selection after drag/right-click, intentional clearing, the group bounding outline, the Selected Objects inspector (with NO canvas-size leakage), exact wall-segment snapping on diagonals (cross-product ≈ 0 — no gaps), structural-beats-angle priority, wall detach/re-angle, the curated 9-item palette with legacy compatibility, structurally distinct furniture symbols, functional furniture color, the unified furniture inspector, side/corner/rotated-bounds resize, text placement/edit/font-size/alignment/bounds/double-click, Floor Settings save/cancel/background/grid/dirty behavior, and Save overlay transitions (idle→saving→saved and idle→saving→error). `FloorEditor.phase1_7.test.tsx` was updated for the Floor Settings dialog flow and Shift-held endpoint angle assistance; `FloorEditor.uxParity.test.tsx` now opens the dialog between canvas resize attempts; `floorPlanNormalization.test.ts` expects the new `backgroundColor`/`showGrid` undo-entry fields.
- Latest B3 final verification and documentation pass (August 8, 2026): manual B3 testing PASSED, including the final campus-card preview check. Automated verification was clean on `feature/building-entrances`: 42 test files / 388 tests passed in `pnpm test`; `pnpm build` PASSED with the existing non-blocking >500 kB `AdminMapBuilderPage` Vite chunk warning only; `git diff --check` PASSED. B3 status is `DONE` on `main`.
- B3 delivered building-attached entrances owned by `CampusBuilding.entrances[]` with `id`, `buildingId`, `edge`, normalized `offset`, optional `name`, `type`, `isPrimary`, and `accessible`. User-facing purposes are General Entrance, Service Entrance, and Emergency Exit. Legacy `main`, `secondary`, `service`, and `emergency` values normalize safely without leaking old labels.
- B3 entrance behavior verified: Add Entrance is building-relative; entrances stay on the perimeter through drag, move, group move, resize, and rotation; entrance clicks select the entrance; building clicks away from entrances select the building; side/position controls are administrator-friendly; add/select/edit/delete/reposition actions are covered; undo/redo/history support is preserved for entrance actions; locked parents block add/edit/drag/delete; hidden parents follow existing editor ghost/public adapter visibility rules.
- B3 Primary rule verified: the first General entrance defaults Primary; additional General entrances are non-primary; switching Primary is one combined state; Service and Emergency Exit cannot be valid Primary; deleting a Primary promotes the first deterministic remaining General entrance only; deleting a building leaves no orphan entrances; duplicate building generates new entrance IDs and remaps `buildingId`.
- B3 validation verified: zero entrances reports "Building has no entrance"; entrances with no valid Primary report "Building has no primary entrance"; exactly one valid General Primary is clean; malformed multiple General Primary entrances are reported; Accessible remains independent from purpose and Primary; Emergency Exit remains data preparation and does not satisfy the normal Primary requirement.
- B3 campus-card fixes verified: initial Map Builder load reads persisted lightweight building footprints through `campusService.list()` and immediately renders a correct building count and visual preview without opening a campus; true zero-building campuses still show "No buildings yet"; positive-count/no-geometry states do not falsely show empty; metadata-only updates preserve the preview summary; saved structure updates refresh the count; metric icons expose accessible/hover/focus labels for Buildings, Floors, Rooms, and Markers.
- B3 persistence/deferred boundary: no new entrance DB migration was added. Entrances are preserved in current editor snapshots and building metadata used by the structure save/load path, and included in shared published snapshots for adapter/search compatibility. Future A5/B6 work should decide the final normalized DB-backed entrance contract and later route-graph integration. B3 does not implement B5 navigation, outdoor-to-indoor routing, accessibility routing calculation, emergency routing, roads, walkways, floors, stairs, or elevators.
- Latest B2 final UI consistency update (August 8, 2026): automated verification is clean on `feature/outdoor-campus-editor` with 40 files / 357 tests passing in `pnpm test`, `pnpm build` passing with only the pre-existing >500 kB `AdminMapBuilderPage` chunk warning, and `git diff --check` clean after the final check. B2 now uses one shared outdoor stack for buildings and decorative assets (`zOrder`), one canonical selected-outdoor-object count, shared PropertiesPanel/right-click layer ordering, hidden editor ghosts while public/student adapters hide hidden outdoor objects, mixed building/decor group movement, and mixed alignment/distribution. The final panel polish makes the right-side PropertiesPanel count match the floating toolbar for mixed selections and makes PropertiesPanel delete remove the selected outdoor object set, including decor.
- Latest B2 manual gate: passed before B3 proceeded and B2 merged through Pull Request #6.

- Active package: B4 — Floor-plan authoring, COMPLETE (branch in use: `feature/floor-editor-overhaul`), status `COMPLETE`; checkpoint commit `feat: complete floor editor overhaul` created. Next: owner decision on merge/push, then B5 (do not start B5 on this branch).
- Last completed package: B3 — `DONE`, merged through Pull Request #7 (merge commit `7b4ba74`)
- Next Developer 2 package after B4: B5 — Navigation graph authoring, according to the implementation plan. Do not start B5 on this branch.
- Important changed files (round 1): `src/lib/campusValidation.ts` (new, extracted validation), `src/lib/pathfinding.ts` (fixed truncated `findPath` reconstruction), `src/lib/__tests__/{pathfinding,indoorPathfinding,mapDataAdapter,campusValidation}.test.ts` + `src/hooks/__tests__/useUndoRedo.test.tsx` (new), `src/lib/__tests__/campusHelpers.test.ts` (added `sanitizeCampus` cases), `package.json` (added `test` script)
- Important changed files (manual-failure repair): `src/lib/editorPlacement.ts` (new: `screenToWorld`, `computeBuildingPlacement`, `shouldDrawNavConnector`, `resetTransientToolState`), `src/components/map-builder/FloorEditor.tsx` (added missing `LandPlot` import — fixed the render crash), `src/components/map-builder/CampusEditor.tsx` (`handleSvgLeave` cancels placement on mouse-leave; `switchTool`/`switchLayer` cleanup; `Escape` clears drag/rubber-band; shared placement geometry), `src/components/map-builder/Canvas.tsx` (`onCanvasLeave` prop; preview uses shared geometry; nav connector gated on real nav data), `src/components/map-builder/useCanvasControls.ts` (`getPoint` delegates to tested `screenToWorld`), `src/lib/__tests__/editorPlacement.test.ts` + `src/components/map-builder/__tests__/FloorEditor.render.test.tsx` (new), `docs/progress/DEVELOPER_2_B1_VERIFICATION.md` (evidence incl. repair round + retest checklist)
- Important changed files (round-2 repair): `src/components/map-builder/useFloorHistory.ts` (rewritten: full floor-state snapshots, reactive `canUndo`/`canRedo`, per-floor reset), `src/components/map-builder/FloorEditor.tsx` (post-change history via `updFloor`; one post-gesture commit on pointer release; `applyEntry` shared by toolbar + Ctrl+Z/Ctrl+Y/Ctrl+Shift+Z with input-focus guard; wall drag translates x1/y1/x2/y2 — was injecting NaN; toolbar disabled states), `src/services/campusStructureService.ts` (finite-number coercion for map_elements x/y/rotation — no NaN→null; walls anchor at x1/y1; descriptive errors naming kind+id; legacy wall normalization on hydrate), `src/components/map-builder/__tests__/useFloorHistory.test.ts` + `src/components/map-builder/__tests__/FloorEditor.undoRedo.test.tsx` (new), `src/services/__tests__/campusStructureService.test.ts` (null-x reproduction + contract compliance), `docs/progress/DEVELOPER_2_B1_VERIFICATION.md` (round-2 evidence §10)
- Important changed files (round-3 repair): `src/components/map-builder/FloorEditor.tsx` (decorative background wrapped in `<g data-bg="true">`; `isBg` now matches descendants via `closest('[data-bg="true"]')` so empty-floor clicks deselect; `data-testid` on wall selection glow + endpoint handles), `src/components/map-builder/__tests__/FloorEditor.selection.test.tsx` (new, 4 tests), `docs/progress/DEVELOPER_2_B1_VERIFICATION.md` (round-3 evidence §11 + B4 follow-up record)
- Important changed files (B2): `src/lib/campusGroupMove.ts` (new, pure group translation/bbox/alignment), `src/lib/campusLayerOrder.ts` (new, pure reorderLayer with no-op detection), `src/lib/decorAsset.ts` (new, normalizeRotation/clampDecorScale/duplicateDecorAsset), `src/lib/decorVisual.ts` + `src/lib/color.ts` (new, shared decor geometry/artwork + shade), `src/components/map-builder/DecorAssetVisual.tsx` (new, shared decor artwork renderer), `CampusEditor.tsx` (drag-group gesture, handleLayerOrder, decor update/delete/duplicate with post-state history, decorRenderScale), `Canvas.tsx` (decor artwork + `data-decor-type`, name-label/scale-badge overlap fix), `PropertiesPanel.tsx` (Layer Order section, full Decorative Asset section with committed inputs, marker-only Layer Order cleanup), `ContextMenu.tsx` (real layer actions + decor branch), `HierarchyPanel.tsx` (shared visual in palette), `constants.ts` (DecorPart[] + redesigned 18 assets), `types.ts` (optional `name` on CampusDecorAsset); 9 new B2 test files (see below)
- B2 tests added (9 files / 82 tests): `campusGroupMove.test.ts` (11) + `CampusEditor.multiMove.test.tsx` (5) [Phase 1]; `campusLayerOrder.test.ts` (17) + `CampusEditor.layerOrder.test.tsx` (7) [Phase 2]; `decorAsset.test.ts` (10) + `CampusEditor.decorProps.test.tsx` (11) [Phase 3 — 21 tests, correcting the earlier verbal miscount of 22]; `color.test.ts` (7) + `decorVisual.test.ts` (9) + `DecorAssetVisual.test.tsx` (5) [Phase 4]
- Important changed files (Create Campus INSERT failure pass): `CampusWizard.tsx` (campus code is now **required** and validated against the DB contract `^[A-Z0-9][A-Z0-9_-]{0,29}$` — the old regex allowed spaces and treated code as optional, so empty/space codes passed the wizard and failed the INSERT `campuses_code_format_check` → "Could not save campus"; also DB-aligned maxLength on name/code/description/address/city/province), `campusService.ts` (`normalizeCampusCode` fails fast pre-INSERT with a clear error; `CampusServiceError` preserves PostgREST `code`/`details`/`hint` with dev-only structured `console.error`; `userFacingCampusMessage` maps known DB codes to concise user-safe messages so constraint names never leak), `AdminMapBuilderPage.tsx` (duplicate-code pre-check before create; all error toasts now pass `{ description }` — sonner 2.x drops a string second arg, which is why users only ever saw the title "Could not save campus" and never the real error), tests: `src/services/__tests__/campusService.test.ts` (new, 11 tests: payload mapping, code validation, error propagation, friendly messages) + 3 new page tests (empty code blocked, space code blocked, duplicate code rejected pre-service)
- B2 known limitations / deferred (not B2 scope): the building/marker/path delete-confirm path still uses the pre-existing pre-state history pattern (can record two snapshots per delete — pre-existing, outside B2; do not migrate broadly); no `tsconfig.json`/`tsc` exists in the repo, so `vite build` is the only compile check (documented repo limitation); markers/nav nodes/edges/events/accessibility objects are intentionally not reorderable (cross-type layer architecture unchanged); `svgPath` on decor descriptors is legacy fallback only; the complete toolbar/tab redesign, campus-card styling, and remaining asset/palette polish stay in B2/B5/B10 follow-ups as recorded in `DEVELOPER_2_B1_VERIFICATION.md` §12.1
- Current B4 boundary: Phase 1 + 1.5 + 1.6 + 1.7 + 1.8 + 1.9 + 2.0 + 2.1 + 2.2 + 2.3 + 2.4 + 2.5 + 2.6 + 2.7 + 2.8 + 2.9 + 3.0 + 3.1 + floor view/default cleanup + text/modal QA + live text editing + floor management workflow/UX consistency + final floor-editor dirty-state integration + final wall-creation structural snapping correction. Floor-plan import/calibration/Measure are dormant/deferred; full wall topology, connector linking, cross-floor stairs/elevator routing linkage, navigation graph authoring, indoor/outdoor route generation, accessibility/emergency routing, and schema/migration changes have not started.
